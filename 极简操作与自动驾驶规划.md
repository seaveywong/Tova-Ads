# ToveAds 极简操作与自动驾驶演进规划

> 2026-09-05 立稿。命题：**用户操作一个地方，链式完成整个 FB 投放；终态是"自动驾驶"——用户只准备主页和账户授权进来，其余全部系统自动完成。**
> 本文档 = 现状链路盘点 + 摩擦诊断 + L0→L5 分级演进设计 + 实施路线图。全部为"进文档"项；文末标注第一个可开工批次。

---

## 0. 愿景终态（L5 画像）

```
用户提供：① BM/账户授权（OAuth）② 主页（或系统从授权里自动发现）③ 素材（甚至只给产品图/落地意图）
系统自动：发现账户 → 纳管 → 建落地页(防护+像素+子码) → AI 生成文案/表单/受众 → 建系列并部署
        → 5min 级止损守护 → 数据回流 → 淘汰差的 → 自动补位新的 → TG 每天一份运营简报
人只做三件事：给素材、设预算池上限、看简报偶尔说"这个方向对/不对"
```

**核心认知：我们已经有了自动驾驶的"下半身"（运行期自动化：Guard 止损/扩量/哨兵/告警/数据回流全在），缺的是"上半身"（冷启动自动化：从授权到第一条广告上线的全链编排）和"小脑"（决策预设层：默认值/档位/护栏）。** 本规划就是补这两块。

---

## 1. 现状操作链路全盘点

### 1.1 从零到第一条广告上线（现状实测路径）

| # | 步骤 | 页面 | 人肉决策点 | 耗时占比 | 可自动化 |
|---|---|---|---|---|---|
| 1 | OAuth 授权令牌 | Tokens | 无（点按钮） | 低 | ✅已是 |
| 2 | 导入账户 | Tokens/Ads | 勾选哪些账户 | 低 | ✅已是（显式纳管 by design，保留） |
| 3 | 上传素材 | Assets | 选文件 | 低 | ✅已是（AI 分析+文案自动） |
| 4 | 建落地页 | Landing | 模板/防护预设/像素/域名 4 类选择 | **高** | 🟡 半自动（有默认，但要人走流程） |
| 5 | 发布+自检 | Landing | 点发布，看自检结果 | 中 | 🟡 已自动跑，但人要盯 |
| 6 | 建子码 | Landing/子码 | 命名 | 中 | 🔴 手动（有 /generate 但要人点） |
| 7 | 建表单模板(LEADS) | FormTemplates | 问题设计 | **高** | 🟡 有 AI 生成，但要人走 |
| 8 | 建投放模板 | LaunchTemplates | 素材/主页/像素/受众/预算/目标 10+ 字段 | **最高** | 🟡 有默认值体系但字段多 |
| 9 | 部署 | LaunchTemplates 抽屉 | 选账户×素材、预检、确认 | 中 | ✅ 已是（含批量/进度/partial 重试） |
| 10 | 配守护规则 | Guard | 规则参数 | 中 | 🟡 有默认规则，但默认未启用 |
| 11 | 绑 TG | Dashboard | - | 低 | ✅已是 |

**总计：跨 6 个页面、约 40+ 个交互点、其中 15+ 个"必须人肉判断"的字段决策。** 老手 30 分钟，新手半天——且每一步都可能在半途（有素材没落地页 / 有模板没子码）。

### 1.2 投放期间（现状）

| 事项 | 现状 | 自动化程度 |
|---|---|---|
| 止损/扩量/哨兵 | Guard 引擎 5min 巡检 | ✅ 全自动（L3 级已达） |
| 告警送达 | 站内+TG（级别偏好矩阵） | ✅ 全自动 |
| 数据回流 | insights/落地/潜客三链 | ✅ 全自动 |
| 上新 | 手动回第 3-9 步 | 🔴 全手动 |
| 素材淘汰 | 规则停，但停了不补位 | 🟡 半自动 |

**结论：运行期已是 L3，冷启动还在 L0-L1。用户感知的"繁琐"几乎全部来自冷启动链。**

### 1.3 四个断点（摩擦诊断）

1. **页面跳转断点**：Assets→Landing→FormTemplates→LaunchTemplates 四页串联，上下文（选了哪个素材/哪个落地页）靠人脑记。
2. **字段决策断点**：模板 10+ 字段每次重填；没有"上次的配置就是这次的默认"。
3. **资产接线断点**：子码/像素/表单这些"中间件"每个都要单独创建再被引用——它们本应是部署时的自动派生物。
4. **确认疲劳断点**：每个环节各有一次确认，而不是**一次对总方案的确认**。

---

## 2. 设计原则（全级别通用）

1. **一个入口**：新增「投放」一级入口（工作台），旧页面全部降级为"高级管理"（管资产/管模板/管规则），不删。
2. **默认值文化**：所有字段有系统默认（预算档位/受众/防护/像素绑定规则），用户从"填空"变"改错"。遵守 no-prefilled 约束的方式：**方案预览卡显示将采用的默认值，输入框留空=用默认**（已有 UX 惯例）。
3. **一次配置处处生效**：默认值集中在「投放预设」（Settings 新节），改一处，全链生效。
4. **花钱节点显式确认的演进边界**：级别越高，确认点越少但越关键（见 §9 边界表）；**任何级别都保留"一键全停"**（已有紧急暂停）。
5. **每级可回退**：自动化的每一步留日志+产物（落地页/子码/模板都是实体资产，可单独删），坏了拔掉自动环节不伤存量。
6. **多租户/权限**：自动驾驶开关是租户级；operator 默认只能用 L2（花钱要 owner 确认）。

---

## 3. 自动驾驶分级总框架

| 级别 | 名称 | 用户做什么 | 系统做什么 | 新增能力 |
|---|---|---|---|---|
| **L0** | 全手动 | 走完 §1.1 十一步 | 提供各环节工具 | （现状） |
| **L1** | 链式向导 | 工作台 3 步：选素材→看方案→确认部署 | 把 4-9 步串成向导，自动填默认 | 投放工作台+预设系统 |
| **L2** | 一键链 | 点一次「投放这批素材到这些账户」 | 编排器自动：建/复用落地页→子码→表单→模板→预检→部署→对账，只停在最终花钱确认 | Smart Chain 编排器 |
| **L3** | 有条件自动 | 设预算池+素材库持续投喂 | 素材进库→自动上新（护栏内）→差的自动停（已有 Guard）→空位自动补 | Autopilot 上新循环 |
| **L4** | 自优化 | 看简报，偶尔调方向 | 自动迭代：文案变体/预算再分配/受众漂移/素材组合淘汰赛 | 优化器（调参+AB） |
| **L5** | 完全自动 | 主页+授权+预算 | 全部含策略层 | （远期，L4 数据充分后） |

> 命名对齐汽车自动驾驶（L0-L5），对外营销语言可直接用"投放自动驾驶 Lx"。

---

## 4. L1 · 投放工作台（One-Flow Console）——砍掉 80% 摩擦的第一刀

### 4.1 交互形态（3 步）

```
投放（新一级入口，平台切 fb/tt 沿用）
┌────────────────────────────────────────────────┐
│ 第 1 步 选素材    素材网格多选（复用批F的批量选择器）+ 每账户部署开关 │
│ 第 2 步 看方案    一张「投放方案卡」：                                             │
│   · 落地页：将创建「{素材名}-LP」（防护=预设/像素=账户已绑/子码=自动）              │
│   · 文案：AI 已按素材生成（可展开改）                                              │
│   · 系列结构：N 账户 × M 素材 = N×M 系列（每系列=素材名，预算=档位默认 $X）        │
│   · 守护：默认规则包（空耗 $20 停 / CPA>1.3×目标 停）已启用                         │
│   · 合计 ≈ $Y/天 ｜ 预检：全部通过（3 黄 0 红）                                    │
│ 第 3 步 确认部署   一个按钮。进度+对账复用现有抽屉。                                │
└────────────────────────────────────────────────┘
```

### 4.2 关键机制

- **方案卡=唯一确认点**：把十一次确认收敛成一次（批量部署的金额确认弹窗已有同款语义）。
- **每个"将创建"都可点开改**：默认折叠，专家可下钻——新手全程不点。
- **复用优先**：落地页/表单/受众按指纹（素材用途+国家+防护级别）查存量，有则复用，无则建——不产生垃圾副本。
- **断点续跑**：向导中途退出，方案存草稿（launch_jobs 已有 job 状态机语义可借）。

### 4.3 工作量与依赖

后端：`/launch/console/plan`（生成方案：串联 asset→landing 方案→模板方案→预检）+ `/launch/console/execute`（转 L2 编排器执行）。前端：新 view + 素材选择器/方案卡/确认三组件。**依赖 §10 预设系统先行。**

---

## 5. L2 · Smart Chain 编排器（自动化的执行底座）

### 5.1 设计

后端新增 `services/launch_orchestrator.py`：

```
pipeline = [
  ensure_accounts(),      # 校验纳管+令牌写权（含权限退避标记检查——今天刚上的机制直接复用）
  ensure_landing(),       # 按 fingerprint 查/建落地页 → 发布 → 自检（fail 则链中断，报告卡在哪步）
  ensure_subcode(),       # 自动生成子码（命名规则=预设，如 {purpose}-{date}-{seq}）
  ensure_form(),          # LEADS 目标时：AI 生成表单（已有）或预设默认表单
  ensure_template(),      # 从素材 AI 文案+预设档位组装模板（不落模板库则为影子模板，可"转正"）
  preflight(),            # 现有预检，汇总到方案
  deploy(),               # 现有批量部署 runner（job/items/partial/重试全套复用）
  verify(),               # 现有部署对账（ads_cache 刷新+live join）
]
```

- **幂等**：每步 ensure_ 前查指纹，跑过的跳过——重试不重建（批F复审里"批量不走同名幂等"的教训只在模板部署层，编排层指纹更粗）。
- **失败补偿**：链在第 k 步失败 → 前 k-1 步产物保留（是资产不是垃圾），下次重跑从 k 步续。
- **全程留痕**：orchestration_runs 表（run_id/步骤状态/产物 id/耗时），前端"链路视图"可视化（哪步绿哪步红）。
- **心跳与回收**：复用 launch_jobs 的 created_at 心跳 + reap 模式。

### 5.2 与现有代码的关系

`launch_templates.py` 的 `_run_deploy_job/_deploy_series_fb/deploy_one_account` 全部原样复用（编排器只多做了"部署前的资产准备"）。**不动花钱链的已验证代码，新增的只是前置编排**——这是本设计风险可控的关键。

---

## 6. L3 · Autopilot 自动上新（授权进来就开跑）

### 6.1 触发模型

```
触发源：① 素材库新增（打标 purpose=投放用） ② 定时（预算池有余量+在投系列数<上限）
动作：取「待投放素材队列」→ 过滤（近 N 天已上过/同素材淘汰过）→ L2 链自动执行
护栏：日预算池硬顶 / 单系列预算档位 / 每日上新条数上限 / 同素材冷却期
```

### 6.2 与 Guard 的攻防一体

- Guard（已有）= 防守端：空耗停/CPA 停/哨兵全停——**不动**。
- Autopilot = 进攻端：补位上新的频率与预算来自 Guard 的淘汰信号（某素材组 CTR 持续差 → 换下一批素材）。
- **闭环关键指标**：在投系列数稳定在目标区间（如每账户 3-5 条活的），死了自动补，好了自动停补。

### 6.3 级别门控

Autopilot 开关（租户级）+ 四个必填护栏参数（预算池/日上限/系列数区间/素材冷却天数）——**无默认值不预填，用户显式设定才生效**（对齐 no-protection-periods 精神：自动化花钱的上限必须显式）。

---

## 7. L4 · 自优化（远期，此处立框架不展开）

- 文案层：AI 已能生成 → 增加变体赛马（同素材多文案并行投，差的 Guard 停）。
- 预算层：扩量规则（roas_scale/slow_scale 已有）从"人设参数"变"策略档位"。
- 受众层：per subscriber 国家表现漂移 → 受众模板自动调权。
- 全部走"生成方案→小流量验证→达标放大"三段，放大仍受预算池约束。

---

## 8. 资产自动接线（授权即就绪，L2 的前置）

用户 OAuth 授权完成后自动执行（可关）：
1. 发现账户（/me/adaccounts）→ 列入"待纳管"清单（**仍显式确认纳管**——account-import-explicit-only 铁律不变）
2. 发现主页（令牌可管的 pages）→ 主页池登记
3. 发现像素 → 像素库登记+按账户绑定关系自动关联
4. 生成默认子码命名规则、默认防护预设档位、默认规则包（空耗/CPA 两条，默认**启用**——参数取行业保守值，遵循"无保护期"：学习期 0）
5. 产出「就绪度清单」：绿灯=可立即被 L2 链使用，黄灯=缺什么（如无像素）

> 纳管确认保留人工：显式导入是资金安全边界（哪些账户可以被系统花钱），这一步永远不自动。

---

## 9. 资金安全红线与确认边界演进表

| 级别 | 建广告确认 | 预算变更确认 | 止损 | 全停 |
|---|---|---|---|---|
| L1 | 确认一次（方案总确认） | 人 | 自动人（已有） | 人（一键，已有） |
| L2 | 确认一次 | 档位内自动 | 同上 | 人（保留） |
| L3 | 护栏内无确认 | 池内自动 | 同上 | 人（保留）+ 池触顶自动停上新 |
| L4 | 同 L3 | 策略内自动 | 同上 | 同上 |

**永不自动化**：① 提高预算池硬顶 ② 解除止损规则 ③ 纳管新账户 ④ 删除历史数据。
**永不降级**：任何级别下 Guard 止损全量生效（autopilot 账户与手动账户同一套巡检）；告警 critical 恒推（TG 偏好矩阵已保证）。

---

## 10. 投放预设系统（L1/L2 的地基，最先做）

Settings 新增「投放预设」节（租户级，system_settings JSON）：

| 预设组 | 内容示例 |
|---|---|
| 预算档位 | 保守 $5/标准 $10/激进 $20（系列日预算，CBO/ABO 各配） |
| 受众模板 | 按用途默认国家组+年龄+兴趣骨架（受众库已有 CRUD，这里只是引用+默认标记） |
| 落地页默认 | 模板/防护预设/域名池策略/像素选择规则（账户已绑>租户默认>新建并提示） |
| 子码规则 | 命名模板 `{purpose}{ymmdd}-{seq}` |
| 规则包 | 空耗 $20/CPA 1.3×目标（默认启用的两条+可加减） |
| Autopilot 护栏 | L3 四参数（无默认，显式填） |

预设被 L1 方案卡/L2 编排器/L3 循环统一读取——**一处改，全链变**。

---

## 11. 实施路线图

| 批次 | 内容 | 依赖 | 量级 |
|---|---|---|---|
| **G1** | 投放预设系统（§10）+ 资产自动接线（§8） | 无 | 小 |
| **G2** | 投放工作台 L1（§4：3 步向导+方案卡） | G1 | 中 |
| **G3** | Smart Chain 编排器 L2（§5：orchestrator+runs 表+链路视图） | G1 | 中大 |
| **G4** | Autopilot L3（§6：上新循环+护栏+攻防联动） | G3 | 中 |
| **G5** | 自优化 L4（§7） | G4 数据积累 | 远期 |

建议从 **G1+G2 合批**起步（预设+工作台直接把冷启动从 30 分钟压到 3 分钟，见效最快）；G3 编排器是通往 L3 的必经之路但可以等 G2 用出反馈后再动。

---

## 12. 现有能力复用映射（已有什么不用重做）

| 规划能力 | 已有基础（直接复用） |
|---|---|
| 链的执行底座 | launch_jobs 异步 runner：job/items/进度轮询/partial 重试/心跳防 reap/部署后对账 |
| 批量建系列 | 批F batchGenerate（模板=母版，每素材一系列） |
| 追踪参数 | ⑥ 静态白名单插值（{{campaign.name}} 等 7 个） |
| AI 文案/表单/受众 | 素材 AI 分析+DeepSeek 文案+Gemini 视觉+AI 表单生成 |
| 落地页工厂 | 模板系统+防护预设+子域名+自检矩阵+发布即检 |
| 像素接线 | 像素自动获取（FB API→库→绑账户） |
| 子码 | /generate 端点+自动清理 cron |
| 防守端 | Guard 规则引擎+哨兵+加白+紧急暂停+告警去重风暴上限 |
| 数据回流 | 巡检顺带 insights+落地日志+潜客轮询三链 |
| 花钱确认 UX | 批量部署确认弹窗（含金额+系列数） |
| 权限退避 | 今日哨兵 perm 退避（写权丢失账户不浪费编排） |

**新造的只有四样：预设系统、工作台 UI、orchestrator、autopilot 循环。**

---

## 13. 对标与差异化

- **FB 原生 Ads Manager**：无链式无自动上新（Automated Rules 只有单点规则，无编排）；我们胜在全链编排+防守闭环。
- **FBInsider**（对标文档）：其强在草稿树编辑器（结构化手工编排），是"更好的手动挡"；我们走"自动挡"路线，错位竞争。
- **1.0 Mira**：五分钟巡检+哨兵思想已被 2.0 继承并加强（本次权限退避/级联回写即证据）；1.0 无冷启动自动化。
- **卖点语言**：「接入即投放（L2）」「素材喂进去，广告自己跑（L3）」。

---

## 附：本文档状态

- 全部内容为**进文档**（规划），未动任何代码。
- 待用户过目定级：① 认可 L1→L3 路线？② G1+G2 是否作为下一开发批次？③ §9 确认边界表是否认可（尤其 L3 护栏内建广告无确认这一条）？
- 关联：`auto-launch-architecture-plan`（2026-07-26 手动投放细化，其素材库/部署抽屉部分已全部上线，本规划是其"后半篇"）、`FBInsider_对标学习.md`、`Mira_Commercial_Plan.md`。

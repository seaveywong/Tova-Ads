# 盘点：编辑器「模板调用位 / conversion_goal / identity」代码侧现状矩阵

- 日期：2026-09-08 · 性质：**只盘点不改代码**（未动任何源文件、未部署）
- 用途：与《蓝图_FB广告管理器创建流.md》（另一 Agent 产出，逐面板字段表）**逐条对账**。§4 预留对账空列。
- 对象：`frontend/src/views/LaunchTemplates.vue`（3465 行，下称 vue）· `backend/app/routers/launch_templates.py`（2922 行，下称 LT）· `backend/app/core/ad_builder.py`（762 行，下称 AB）· `backend/app/core/ad_ops.py`（下称 AO）
- 姊妹篇：《审计_编辑器UX与逻辑.md》（UX 全量问题 33 项）；本文档只管三题的**代码侧实况**（调用位/门/消费点/FB 字段），问题编号直接引用审计（A1/A2/C2/C4/C5/D2…）。

---

## 0. 前置事实（对账前必读，防止把死分支当活代码）

**0.1 模式可达性**
- **FB 模板强制树（结构）模式**：openEdit 末尾 `if (!isTt && editMode==='flat') _synthTreeFromFlat()`（vue:1280-1281）；模式切换控件已从 UI 移除（`onModeSwitch`/`flatFromTree` 定义于 vue:971/985 但无引用）。
- 平铺 FB 唯一可达入口：广告列表「复用此帖铺放」→ query `reuse_post` 预填，显式设 `editMode='flat'`（vue:355-362）。**所有其它平铺 FB 分支（组段优化/计费/目的地/内联受众/版位/vue:2203-2213 等）实际不可达**。
- TT 模板恒平铺（LT:1256 `!isTt` 才解析 structure；后端 400 拦 TT 结构）。

**0.2 部署链三入口（FB）**

| 入口 | 函数 | 触发 | 备注 |
|---|---|---|---|
| 树部署 | `LT:_deploy_item_fb_tree`（LT:1937） | 树模板 deploy | 组级 build_adset LT:2069-2085；广告节点块 LT:2097-2300 |
| 平铺/批量系列 | `LT:_deploy_series_fb`（LT:1793）→ `AO:deploy_one_account`（AO:153） | 平铺单发 / 批量逐素材 | AO:202-216 build_adset |
| 预检 | `LT:_preflight_tree_fb`（LT:913） | 部署前弹窗 | payload 样例 LT:1010-1037（与 runner 有已知漂移 D1/D2） |

**0.3 树 runner 组级 pixel 已接**（审计 S4 已修）：LT:2073 `snode.pixel_id > item.pixel_id > tpl.pixel_id`（commit b127641）。对账时勿沿用审计里「像素恒模板级」的旧口径。

---

## 1. 矩阵 A：五个模板调用位（核心矩阵）

### 1.1 表单模板 lead_form_template_id

| 维度 | 树模式（FB 主路径） | 平铺模式 |
|---|---|---|
| UI 位置 | 广告节点卡「Instant Form」段，节点级下拉（vue:2651-2657，`setNodeFormTpl` vue:1085） | 广告段「Instant Form」模板级下拉（vue:2514-2527） |
| 显示条件 | `form.objective==='OUTCOME_LEADS'`（vue:2651，无 isTt 门，但 TT objective 枚举不同实际不渲染） | `OUTCOME_LEADS && !isTt`（vue:2514）→ **TT LEADS 绑不了表单 = 审计 A2** |
| 选项/联动 | `formTemplatesForPlat` 按平台过滤；label 带 `✓`=已有 fb_form_id；选中回填 `lead_form_id=fb_form_id`（vue:559-565） | 同左 |
| 存储列 | 节点 JSON `ads[].lead_form_template_id` + 模板列 `lead_form_template_id`（model:47）/`lead_form_id`（model:44） | 同左（模板列） |
| 后端消费 | `_resolve_lead_form`（LT:1452）：①模板 id → 同载体复用（`fb_page_id==page_id`）否则 `build_lead_form_payload` 建到目标主页（LT:1514-1556）；②手填 `lead_form_id` 直接用不校验载体（LT:1505/1567）。树：节点>模板回退（LT:2153-2154）；门 `objective=='OUTCOME_LEADS' && page_id`（LT:2163） | 同函数（LT:1819-1825），失败 `except: pass` 不阻断 |
| AI 兜底 | 无模板+无 form_id → `_ai_auto_create_form`（LT:1575，AI 按素材文案建表单） | 同左 |
| FB 字段 | `build_creative` `lead_form_id` → CTA 固定 `SIGN_UP` + `link_data.call_to_action.value.lead_gen_form_id`（AB:360-364） | 同左 |
| 缺口判定 | ✅链路完整（FB）；⚠ A2：TT 侧 UI 挡了而后端 `build_tt_lead_form_payload`（LT:1484/1613）就绪；⚠ 解析失败静默 pass（LT:1824/2169，FB 用默认表单或报错） | 同 |

### 1.2 消息模板 message_template_id

| 维度 | 树模式 | 平铺模式 |
|---|---|---|
| UI 位置 | 广告节点卡「消息广告」段，节点级下拉（vue:2643-2649，`setNodeMsgTpl` vue:1084） | 广告段模板级下拉（vue:2499-2507）+ 预览条（2508-2511） |
| 显示条件 | `form.objective==='OUTCOME_ENGAGEMENT'`（vue:2643） | `OUTCOME_ENGAGEMENT && !isTt`（vue:2499） |
| 选项/联动 | `msgTemplates` 全量，label 带 `[Messenger]/[WhatsApp]` chip（vue:558）且**注释明说不过滤 type**；选中平铺侧序列化 `{text,ice_breakers}` 进 `message_template` 原始 JSON 列（vue:566-573）；树侧只存 id | 同左 |
| 存储列 | 节点 `ads[].message_template_id` + 模板列 `message_template_id`（model:48）/`message_template` raw JSON（model:43） | 同左 |
| 后端消费 | 节点 id → `MessageTemplate(welcome_text, ice_breakers_json)` → JSON（LT:2173-2186）> 回退 `tpl.message_template`（2188）> AI 素材文案/跟帖帖文兜底（2189-2200）→ **全部汇入 `message_template` 局部变量** | 同构（LT:1827-1839）→ `AO:deploy_one_account(message_template=…)` |
| ⚠ 生死门 | `is_messaging = objective∈{ENGAGEMENT,…} && conversion_goal∈{conversations, messaging_purchase_conversion, messaging_appointment_conversion}`（树 LT:2224-2226 / 平铺 AO:230-232）。为真才：①查主页 messaging_feature_status（LT:2227-2236/AO:233-242）②`parse_message_template`（AB:716）→ welcome_msg | 同左 |
| FB 字段 | `link_data.page_welcome_message` / `video_data.page_welcome_message`（AB:380-382/400-401）；is_messaging=false 时**根本不进 payload** | 同左 |
| 缺口判定 | 🔴 **死链（审计 A1 实锤）**：门要求的 conversion_goal 取值 UI 永远给不出（见 §2）；且门不看 `optimization_goal`/`destination_type`（用户经优化目标覆盖可达 CONVERSATIONS（OPT_GOALS vue:130-135 含之），欢迎语仍不注入，连 messaging 能力检查也跳过） | 同 |

### 1.2b WhatsApp 消息模板（type=whatsapp）专项

| 维度 | 现状 |
|---|---|
| 定义 | `MessageTemplate.type ∈ {messenger, whatsapp}`（models/lead_form_template.py:39，注释「文案语义不同，结构同」） |
| UI | 仅 label chip（vue:558），无类型过滤、无 WhatsApp 专属绑定位 |
| 部署链消费位 | **无**。两处消费（LT:2184-2186 / vue:572）都只取 `welcome_text`+`ice_breakers` → `page_welcome_message`（Messenger 专属字段）。`mt.type` 全链零读取 |
| WhatsApp 目的地配套 | 无：无 WABA/号码身份输入、无 `WHATSAPP_MESSAGE` CTA 构造。唯一痕迹 = `DEST_TYPES` 含 `WHATSAPP`（vue:142，平铺目的地覆盖下拉 vue:2205，FB 分支不可达）→ `destination_type_override` 裸透传（AB:282-283），promoted_object/creative 无配套 |
| 缺口判定 | 🔴 WhatsApp 模板=摆设（选了只会以 Messenger 欢迎语形态消费，且又撞 1.2 的 is_messaging 死门）；WHATSAPP 目的地=裸透传未验证 |

### 1.3 落地页 landing_page_id

| 维度 | 树模式 | 平铺模式 |
|---|---|---|
| UI 位置 | 广告节点卡「落地页」节点级下拉（vue:2630-2635）+「落地 URL」手填（2636） | 广告段下拉（vue:2484-2488）+ URL（2490） |
| 显示条件 | 恒显（AWAIRENESS 除外另有保存校验 vue:392） | 同左 |
| 联动 | 选页 → `onNodeLandingChange`（vue:1073-1082）自动填 `landing_url=public_url` + 拉该页子码 + 换页清旧子码 | `onLandingChange`（vue:335-351）同 |
| 后端消费 | **部署链零消费 landing_page_id**——只用 `landing_url`（节点 anode.landing_url / tpl.landing_url）经 `_interp_landing_url` 占位符插值（LT:694，{{campaign.name}}/{{adset.name}}/{{account.*}}/{{asset.name}}，树 LT:2211-2215）。列仅用于存储/回显/复制（LT:246 复制白名单）与回显时拉子码 | 同 |
| FB 字段 | `link_data.link` = effective_url（AB:355/367） | 同 |
| 缺口判定 | 🟡 纯前端联动糖：落地页在 LP 库改了 URL，模板 `landing_url` **不随之更新**（用户须重选一次才刷新）；后端无校验 landing_page_id↔landing_url 一致性 | 同 |

### 1.4 子码 subcode_slug

| 维度 | 树模式 | 平铺模式 |
|---|---|---|
| UI 位置 | 节点级下拉（vue:2637-2641），选项按该节点落地页过滤（`subcodesForNode` vue:1068-1070），label 带状态 | 模板级下拉（vue:2492-2496，`subcodesForLanding` vue:545-547） |
| 显示条件 | 恒显（选中落地页才有选项） | 同左 |
| 后端消费 | 节点 slug → `_subcode_cache` 查 `LandingAdLink`（LT:2114-2121）→ `effective_url = {base}/a/{slug}?ad={ad.id}`（LT:2216-2220，FB 宏）→ `link_data.link`；建 ad 后回绑 `subcode_link.ad_id`+`status=active` + 广告名标注 `[子码:slug]`（LT:2269 起，同构 AO:279-285） | `_run_deploy_job` LT:2350-2352 查 link → `AO:224-226` effective_url + 回绑 |
| 预检 | 🔴 **树预检无子码校验**：`subcode_warn_slug` 只在平铺分支返回（LT:787-794）/渲染（vue:2902）；树部署注释自称「预检已拦」（LT:2113）实未拦 → 无效 slug = link None = **静默丢追踪**（审计 D2） | ✅ 有（LT:787-794 + vue:2902 警示） |
| 缺口判定 | 🟡 链路本身完整（URL→宏→回绑→改名）；缺口在树预检（D2） | ✅ |

### 1.5 跟帖 post_source=reuse + reuse_post_ref

| 维度 | 树模式 | 平铺模式 |
|---|---|---|
| UI 位置 | 广告节点卡「创意来源」radio（vue:2560-2563）+ 引用输入/浏览按钮（2565-2573，浏览需先选主页 vue:2571）+ 迷你预览（2574-2585，`nodePostPreviews`） | 广告段顶部 seg（vue:2389-2394）+ reuse 卡：输入/浏览（2395-2408）/URL 识别（vue:1103-1120 正则抽 post_id）/预览（2409-2447）；主页下拉被锁（vue:2045 `disabled` + `pageLockedByPost`） |
| 显示条件 | 恒可选（FB）；reuse 强制单素材（vue:1088-1093 拦多素材） | 同左；query `reuse_post` 预填入口（vue:355-362，来自广告列表） |
| 联动 | 输入 ID/URL → `/fb/resolve-post`（LT/vue:1102/657-694）识别主页+内容预览；识别失败揭示手选主页（vue:627/2402-2408） | 同 |
| 后端消费 | `_resolve_page_post`（LT:1652-1677）：reuse → 跨主页校验（1659-1662，帖主页≠目标主页报错）→ 返 ref；**new 路径仅 dev app 建新帖**（1670-1671，standard 撞 code3 返空走 object_story_spec，见 auto-memory keepalive-creative-fix） | 同（LT:1845） |
| FB 字段 | `POST /adcreatives {object_story_id, call_to_action}`（树 LT:2250-2253 / 平铺 AO:258-261）→ creative_id → ad | 同 |
| 账户过滤 | `/launch-templates/{id}/reuse-eligible`（LT:724）→ 前端 `reuseEligibleActs` 按主页权限灰账户（vue:1507-1515/2801-2807）；部署 hint（vue:2777） | 同 |
| 附带 | reuse 无素材时帖文当消息欢迎语兜底素材（LT:1836-1837/2197-2198）——又被 §1.2 死门挡 | 同 |
| 缺口判定 | ✅ 五个调用位里完成度最高（识别/校验/预检过滤/预览全有） | 同 |

---

## 2. 矩阵 B：conversion_goal（三套词表互不重叠 = 两个死链的根因）

**2.1 UI 现状**
- 输入：系列段「转化目标」下拉（vue:1990-1993），`v-if="convGoalsForObjective.length"`。
- 值域 `CONV_GOALS`（vue:151-158，注释自称「FB custom_event_type 枚举」——即 **Pixel 自定义事件名**）：
  - SALES：Purchase/AddToCart/InitiateCheckout/AddPaymentInfo/CompleteRegistration/Lead/Subscribe/Contact/StartTrial/Search（10 值）
  - LEADS：Lead/CompleteRegistration/Contact/Subscribe/Search/StartTrial/Purchase（7 值）
  - **TRAFFIC / ENGAGEMENT / AWARENESS：`[]`（下拉不渲染）**
  - APP_PROMOTION：APP_INSTALLS/LEVEL_ACHIEVED/ACHIEVEMENT_UNLOCKED/SPENT_CREDITS（但该目标链路不支持，审计 C3）
- 自动填：objective watcher（vue:171-185）按 `OBJ_DEFAULTS.conv`（vue:162-169）填 SALES→Purchase、LEADS→Lead；只在「空值或旧默认」时覆盖 → 跨目标残留（审计 C4）。

**2.2 后端消费点（三处，词表各不相同）**

| 消费点 | 位置 | 期望词表 | UI 词表交集 |
|---|---|---|---|
| ① `get_optimization_goal` 推断 | AB:96-100 查 `_OPT_GOAL_MAP`（AB:20-55），key=**目的地语义** `offsite_conversions/lead_generation/page_likes/post_engagement/link_clicks/reach/impressions/conversations/landing_page_views/messaging_purchase_conversion/messaging_appointment_conversion` | 上述 | **零交集** → 永远落到各 objective 的 `""` 默认（SALES→OFFSITE_CONVERSIONS、LEADS→LEAD_GENERATION、ENGAGEMENT→REACH、TRAFFIC→LINK_CLICKS）。UI 选「转化目标」对优化目标推断无任何影响（审计 C2） |
| ② `is_messaging` 门 | AO:230-232 / LT:2224-2226，要求 ∈ {conversations, messaging_purchase_conversion, messaging_appointment_conversion} | 消息类 | **不可达**：ENGAGEMENT 下 UI 下拉不渲染（§2.1），conversion_goal 只能是 ''/残留值 → 消息模板死链（审计 A1） |
| ③ `build_creative(conversion_goal=)` | AB:334→353，仅用于 opt_goal 计算（同①词表） | 同① | 零交集 |

**2.3 custom_event_type 恒定实锤（行号）**
- `build_adset` 有形参 `conversion_event: str = "PURCHASE"`（AB:163），但 **全后端无任何调用方传它**（grep `conversion_event=` 仅命中落地页链路 landing.py/landing_events.py，与广告部署无关）。
- SALES 分支：`"custom_event_type": conversion_event or "PURCHASE"`（AB:230-237）→ **恒 PURCHASE**。
- LEADS-网站分支（opt_goal=OFFSITE_CONVERSIONS 时）：字面量 `"custom_event_type": "LEAD"`（AB:246-254）→ **恒 LEAD**。
- 三个 build_adset 调用位均不传：AO:202-216（平铺）、LT:2069-2085（树 runner）、LT:1015-1026（树预检）。
- 结论：**UI「转化目标」下拉与 FB `promoted_object.custom_event_type` 完全脱钩**（审计 C2 实锤）。用户选 AddToCart 部署出来仍是 PURCHASE 优化。
- 附带：ENGAGEMENT+消息类 opt_goal（CONVERSATIONS 等）经 `_OPT_GOAL_MAP` 可推断（AB:36-38/265-270：promoted_object=page_id、destination_type=MESSENGER、targeting.publisher_platforms=[messenger] AB:286-287）——但前提是 conversion_goal 给得出对应值或用户手动覆盖 optimization_goal；后者可达（树组覆盖 vue:2148-2153 / 平铺 vue:2203），此时 destination/promoted_object 正确、唯独欢迎语死（§1.2）。

---

## 3. 矩阵 C：identity（主页 / IG / WhatsApp）

| 身份 | UI 位置 | 显示条件/形态 | 存储与部署消费 | FB 字段 | 缺口判定 |
|---|---|---|---|---|---|
| 主页 page_id | 系列段下拉（vue:2044-2049，选项=令牌可管理主页 `tplPages`）；跟帖平铺时锁死（disabled，主页由帖定） | 模板级全局单值；**树模式无 per-ad 主页**（审计 C5，注释亦自知 vue:2553） | model:37；部署 `item.page_id（抽屉 per-account 覆盖，vue:2822-2826/LT:1796 组装）> tpl.page_id`（LT:1812/1999）。抽屉另有「随机分配主页」全批去重（vue:1644-1668）。promoted_object 与 creative 共用同一 page_id | `object_story_spec.page_id`（AB:385）+ `promoted_object.page_id`（AB:244/260/269）+ object_story_id 路径的 CTA value.page | 🟡 形态偏离 FB（FB 在广告层选身份、可 per-ad）；多主页轮换已有随机分配兜着，单广告节点级无 |
| Instagram instagram_actor_id | **仅树模式**广告节点卡「身份」段裸 ID 输入（vue:2553-2557，注释：模板级全局身份、无节点字段）；平铺分支无输入框（但平铺 FB 不可达）；TT 不适用 | 可选；后端校验纯数字（LT:404-409 `_check_ig_actor`） | model:69；payload 直传（vue:1379）；部署 `build_creative(instagram_actor_id=tpl.instagram_actor_id)`（LT:1890/2245） | `object_story_spec.instagram_actor_id`（AB:407-410；空=不传，FB 用主页关联 IG） | 🟡 裸 ID 非 IG 账号选择器（审计 A7）；无「主页关联 IG」列表数据源，用户无从查 ID |
| WhatsApp | **无任何输入位** | — | 无存储列；无部署消费 | （FB 侧 WhatsApp 广告需 destination_type=WHATSAPP + 对应 CTA/身份，全无构造） | 🔴 完全缺失：无 WABA/号码身份、无 WhatsApp 目的地配套（DEST_TYPES 有 WHATSAPP 选项 vue:142 但 FB 分支不可达；透传点 AB:282-283 无配套字段）→ 与 §1.2b 合并为「WhatsApp 整链不存在」 |

---

## 4. 蓝图对账空表（蓝图到达后逐行填）

用法：蓝图每列出一个面板/字段，在此追加一行，回填「蓝图要求」与「差距/处置」。

| # | 蓝图面板.字段 | 蓝图要求（待填） | 现状锚点（本文档 §） | 差距（待填） | 处置 P0/P1/P2/接受（待填） | 对应审计编号 |
|---|---|---|---|---|---|---|
| 1 | | | §1.1 表单模板 | | | A2 |
| 2 | | | §1.2 消息模板（is_messaging 死门） | | | A1 |
| 3 | | | §1.2b WhatsApp 消息模板 | | | （审计未列） |
| 4 | | | §1.3 落地页（后端零消费） | | | （审计未列） |
| 5 | | | §1.4 子码（树预检缺口） | | | D2 |
| 6 | | | §1.5 跟帖 | | | — |
| 7 | | | §2 conversion_goal / custom_event_type | | | C2/C4 |
| 8 | | | §3 主页 | | | C5 |
| 9 | | | §3 IG | | | A7 |
| 10 | | | §3 WhatsApp | | | （审计未列） |
| 11 | | | | | | |
| 12 | | | | | | |

---

## 5. TOP5 缺口（对账时的重点核对面）

1. **conversion_goal→custom_event_type 断链**（§2.3）：`conversion_event` 形参全后端无人传，SALES 恒 PURCHASE（AB:235）/LEADS-网站恒 LEAD（AB:252）；且 UI 词表（Purchase/AddToCart…）与 `_OPT_GOAL_MAP` key（offsite_conversions…）零交集 → 「转化目标」下拉纯摆设（审计 C2）。若蓝图要求选转化事件生效，需同时改 UI 词表或建映射。
2. **消息模板死链**（§1.2）：is_messaging 门要的 conversion_goal 值 UI 永远给不出（CONV_GOALS.OUTCOME_ENGAGEMENT=[]，下拉不渲染）→ `page_welcome_message` 永不注入、主页 messaging 能力检查也跳过；门不看 optimization_goal/destination_type，即使用户把优化目标覆盖成 CONVERSATIONS 也救不回（审计 A1）。
3. **WhatsApp 整链不存在**（§1.2b+§3）：消息模板 type=whatsapp 无独立消费位（welcome_text 直接进 Messenger 专属 page_welcome_message）；无 WhatsApp 身份输入、无目的地配套。选 WhatsApp 类模板/目的地当前都是误导性 UI。
4. **identity 形态偏离**（§3）：主页=模板级全局单值（树模式无 per-ad 主页，靠部署抽屉随机分配兜底）；IG=仅树模式的裸数字 ID 输入（无选择器、无关联 IG 列表数据源）。与蓝图「广告层身份选择」对照差距最大。
5. **落地页/子码链的静默面**（§1.3/§1.4）：landing_page_id 后端零消费（LP 库改 URL 模板不跟随）；树预检无子码存在性校验（无效 slug 静默丢追踪，注释自称已拦实未拦）。

---

*生成方式：grep 定位 + 窄窗口读码，未整读任何大文件；未改任何源文件。行号以 2026-09-08 工作区为准（HEAD 137e860）。*

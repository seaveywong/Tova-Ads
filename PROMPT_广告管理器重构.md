# ToveAds 2.0：FB 广告管理器 1:1 复刻执行提示词

## 最终目标与授权

把 ToveAds 2.0 的 FB 广告管理器做到与 Facebook Ads Manager 对标一致：视觉布局、三层操作、指标口径、完整交互和真实后端全部接通，并成功部署、验证。不要交付静态仿图、假数据界面、只有按钮的空壳，也不要把 build 成功或 API 200 当作任务完成。

用户已授权：找不足直接修改、实现对标功能及必要的页面/接口重构，持续做到验收完成；无需为范围内的常规修复反复确认。保留 TikTok、多租户、实时核验和潜客功能。不要扩展到无关产品、自动驾驶规划、1.0 server_src/、引擎整体重构或数据库 schema 变更。资金链继续使用已验证执行函数，只补前置校验和结果呈现。

这是完成目标，不是成功保证；遇到真实平台权限/API限制，要记录证据、准确暴露受限状态并明确未完成项，不能用假成功掩盖。不要因为旧提示词只列16项就把1:1目标缩减为修几个P1。

## 先阅读，后动手

工作目录 D:/dev/Mira_One，Git仓库在 toveads/。先看 git status/log，保留其他人的改动。

1. HANDOFF.md → ARCHITECTURE.md → CLAUDE.md → CREDENTIALS.local.md（最后一份只本地读取，不能回显或提交凭据）。
2. 交接_广告管理器本轮收尾_20260909.md：最新代码状态、测试证据、未完成项。
3. 交接_广告管理器现状.md → 蓝图_FB广告管理器创建流.md → 系统串联全景.md。
4. PROMPT_广告管理器重构.md（本文），以及下面当前源码：
   - frontend/src/views/AdManager.vue
   - frontend/src/composables/adManagerView.js 及其测试
   - frontend/src/locales/views/admanager.js
   - backend/app/routers/ads.py
   - backend/app/services/ads_cache_sync.py
   - backend/app/services/guard_engine.py 中缓存回写和表现快照写入部分
   - _smoke_ad_manager.py

**接续基线：f8dc294 已推送 main，未部署。** 已有三层合并表格、列配置、部分面包屑/搜索/预算编辑，以及后端成效汇总，不要重新从零实现，也不要直接部署这个尚未浏览器验收的版本。历史文档中的“没有列配置”等是旧现状，需以当前代码和实际运行结果重建对标表。

## 先建立真正的 FB 对标基准

优先在用户已有可用会话中只读查看 FB Ads Manager，固定参考账户、语言、视口、日期范围和权限；保存必要截图及交互证据。无法访问时，使用项目蓝图和官方资料，明确哪些没有实测，禁止声称亲自看过。

建立持续更新的对标矩阵：FB参考行为 / 当前行为 / 具体差距 / 实现文件 / 验证证据 / 已验收或受阻。最低覆盖下列16项，并补充实际观察到的核心差距：

1. 系列、广告组、广告三层表格与视图切换。
2. 状态开关及权限、不可操作状态。
3. 批量选择、确认、逐项反馈、失败重试。
4. 日/总预算统一行内编辑及回读核验。
5. 改名及成功/失败反馈。
6. 日期范围、账户时区及数据口径。
7. 跨账户/父层搜索、过滤及清除过滤。
8. 多列排序、状态优先级和刷新记忆。
9. 列显示/隐藏、顺序、预设、宽度及持久化。
10. 年龄、性别、地区、版位、设备细分（真实API数据）。
11. 从实体行创建规则，范围精确落到该系列/组/广告。
12. 父子钻取、可返回面包屑及切账户/平台后的上下文。
13. FB成效、综合转化、费用和汇总，不能串口径。
14. 脱管、被禁、令牌失效、过期快照、拒登原因。
15. 保留实时核验，明确核验层级、时刻和失败状态。
16. 保留潜客Tab及其原有完整操作。

继续核对工具栏、筛选器、表头/固定列、密度、菜单、详情与预览、空态、加载态、错误态、键盘操作和窄屏表现。对于创建/编辑流程等关联入口，验证现有入口贯通；对标发现的核心差距应加入矩阵并完成，不可留下无效入口后宣称1:1。

视觉验收用相同视口的参考图和实现图逐项对比；不能只凭“看起来像”。保留 ToveAds 品牌和已有必要能力，不伪装成 Meta 官方产品。

## 推荐执行顺序

A. 重跑受保护的基线smoke并恢复测试临时状态；先浏览器检查 f8dc294，定位合并表格的运行时问题和丢失入口。
B. 先修数据与身份：租户/平台/账户/实体隔离、历史results_fb默认0、真实快照时间、币种、日期、汇总与FB对账。综合转化绝不能冒充FB成效；不能把未知显示为已确认0。
C. 完成三层导航、全局搜索边界、列自定义、排序记忆、选择状态、预览和状态提示；避免下钻/日期/账户切换留下隐形选择。
D. 完成资金操作前置校验、预算专用未核验提示、批量逐项隔离和verified计数；Live Verify按实际核验层级展示，解决跨worker刷新状态。
E. 完成真实细分和行内实体规则。规则必须在保存、编辑、诊断、执行路径一致限定实体范围，绝不能默默扩为整账户；测试时不能动真实投放。
F. 按FB参考逐项修视觉和交互、回归TikTok与潜客，再走完整部署验收。按可验收批次推进，但持续工作直到最终目标达成。

## 铁律

- 无学习期/保护期/宽限，默认0；不要删除带事故原因的防御代码。
- 输入框不预填默认值；现有值可用于占位提示，按既有业务规则验证。
- zh/en同步、en零CJK、新文案实际运行时编译；Vue插值语法正确，属性用绑定；不使用彩色emoji。
- emit_notification必须有dedup_recent；后端新增错误进入error_i18n译表。
- 不重写已验证的部署/暂停/预算执行函数；真实写操作必须回读，verified=false不能显示成功。
- 默认需关注项优先；保留已确认的脱管/不可操作账户置底分组规则，普通列排序不能覆盖分组。
- 多租户权限和账户/平台身份不能因复刻UI而放宽；缺数据、无权限、部分失败必须明确显示。
- 验证资金操作用mock或明确的测试对象，不得为了截图/测试随意修改真实广告预算、状态或投放。
- 先commit后部署。build失败立即停止；必须确认当前产物包含本次代码，不能上传旧dist。

## “成功”的验收标准

必须全部具备证据：

1. 对标矩阵覆盖完整，核心功能真实可用；未验证不能标已完成。
2. 实际浏览器跑完中英文、三层导航、过滤/搜索、列配置刷新记忆、批量混合结果、预算未核验、损坏图片、空态/错误态、窄屏和旧功能回归。截图之外还要验证操作结果。
3. 后端不仅200：用确定数据断言广告→组→系列聚合、FB与综合转化差异、缺失/0、多币种、日期、跨租户/平台隔离及操作回读。
4. 本地检查通过 → 当前服务器代码差异核对及代码/DB备份 → commit → 部署双门（py_compile+import）→ restart → /health → 新版数据断言smoke → journalctl无新引入错误。
5. 前端重新build并验证产物，Cloudflare Pages项目tovaads、生产分支master；核对线上实际加载的新资源及关键页面行为。仓库分支main不等于Pages生产分支。
6. commit+push，更新TECH_REVIEW、现状/对标矩阵、后续交接，记录提交号、部署版本、测试结果、证据位置和限制。全库语言门不能因为“存量”就直接忽略；先用实际编译区分误报与真问题。

最终汇报：完成哪些FB对齐、部署提交与线上版本、真实验证证据、剩余限制。没有完成部署和验收，就写未完成，禁止说“1:1成功”。现在直接接手执行，不要停在计划或询问要不要继续。


## 创建与编辑专项实施规格

用户追加要求已整理为 [实施规格_FB广告创建与编辑_配置及API映射.md](实施规格_FB广告创建与编辑_配置及API映射.md)：含完整层级、配置说明、字段依赖、Meta API映射、创建/编辑差异、API条件限制及执行提示词。下一位实现创建编辑时先读此文；本次只新增文档，未做平台写入或部署。

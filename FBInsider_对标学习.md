# FBInsider 对标学习笔记（TovaAds 2.0 借鉴清单）

> 2026-09-03 · 竞品：fbinsider.com（Laravel Filament 后台，zh_CN，私域工具非 SaaS）
> 方法：登录会话（Livewire 协议）实测全站 16+ 页面，含弹窗内容解码（非表面文案）。
> 四份分报告：`../_fbi_A_ads.md`（广告管理）/ `../_fbi_B_creative.md`（创意草稿）/ `../_fbi_C_bm_tg.md`（BM+TG）/ `../_fbi_D_dash_commercial.md`（看板+商业化）。

---

## 一句话总览

FBInsider 是**重操作台、轻数据**的私域投放工具：操盘动效（草稿树/批量生成/行内编辑/异步任务中心）比我们强一档；数据口径透明化和 TG 通知工程闭环值得整体照抄；**但零商业化、零 AI、零素材回流、无落地页体系**——TovaAds 的 SaaS 架构、AI 文案、落地页闭环、双平台（TT）、多租户 RLS 全是我们领先的代差点。

---

## P0 立刻值得抄（5 项）

### 1. batchGenerate「按素材批量生成系列」⭐ 全站最锋利
系列节点上一键弹窗 → 选 N 素材（上限 200）→ 填「每系列 N 组 × 每组 M 广告」→ 以母版系列克隆（组复制预算/定向、广告复制文案、**广告名=素材名**、素材不足不建空组）。算例直给："1 组 × 3 广告，60 素材 → 20 系列"。
**我们差距**：批量部署只有账户维度，没有结构维度。素材→广告的映射靠手动。
**落点**：投放模板部署抽屉加「按素材批量」模式；模板即母版，素材列表勾选即数量。

### 2. Operation 模型 = 异步任务中心 +「部分失败」一等状态
发布草稿/批量改状态/预算/改名/删除等 9 类操作全部入队，进度格式「成功 5 · 失败 1 · 共 6」，详情页**「重试失败项」只补败的不重跑全部**，统一 Operation 日志可查。
**我们现状**：批量部署已有一半（进度+重试），但没泛化到所有批量写（改名/预算/暂停）。
**落点**：guard_engine/批量操作的统一异步化改造，"部分失败"进一等状态机。

### 3. 行内编辑即操盘核心（零跳转）
三级列表每行：「关/开」Toggle 直切状态、铅笔改名（提示"不触发重审"）、铅笔改预算（按账户原币种，Enter 即存）；**名称本身是钻取按钮**（系列→组→广告）。
**我们现状**：操作多在抽屉/弹窗里。AdManager 可平移为行内化。

### 4. 数据口径透明化（正中我们三个已知痛点）
内部模型把"数据不可全信"全部建模并**展示出来**：
- `latest_day_partial`（今日实时未结算标记）—— 对应我们的快照滞后
- `approx_fx`（汇率近似）—— 对应我们汇率硬编码债
- 账户行显示**实际数据跨度**（查 30 天只显示有数据的 08-20~08-24）—— 消灭"零消耗 vs 无数据"歧义
- 双币种双列（原币 + 约 USD）；KPI 卡副文本处处标注（去重/约/仅供参考）
- 独立「显示偏好」页：换算币种 + 显示时区（P1 顺带解汇率债）

### 5. TG 通知工程闭环（骨架整体照抄）
绑定：**32 字符一次性深链 token**（15 分钟有效仅限本人，/start 后 bot 回确认 + 测试消息 + 解绑）→ 监控列表做**账户白名单 × 4 类告警行内 Toggle 矩阵**（默认不推任何账户，用户显式圈定）→ 通知设置类型主开关 + 阈值参数 → 推送走队列（待发送/已送达/失败 + attempts 重试 + 失败原因 + 30 天留痕）→ 发送记录页自助排查。三层门控文案极显式。
**我们现状**：TG 刚修通（深链短码/多绑定），缺：账户白名单矩阵、发送留痕页、队列重试。另值得抄**花费骤变告警参数**（7 天同期均值 × 3 倍阈值 × $50 差额下限防小额噪声）——guard_engine 缺这个维度。

---

## P1 该做（6 项）

| # | 借鉴点 | 说明 |
|---|---|---|
| 6 | **草稿树编辑器** | 草稿=与账户解耦的完整三层结构树（campaign→adset→ad），左树右表单，节点行首开关控制"发布后是否直接投放"（整链开启才消耗）、完备度圆点、Ctrl 多选 + 跨草稿复制粘贴、400ms 防抖自动保存、发布向导含预检横幅（哪些节点将立即消耗）+ 逐账户主页绑定（一键应用全部）+ Pixel 自动创建。我们的平铺模板信息架构低一档 |
| 7 | **账户分组** | 极简打标签（单归属）→ 列表筛选 + 花费按组汇总。我们完全没有 |
| 8 | **同步状态灯** | 顶栏 10s 轮询徽标（同步空闲/进行中/上次同步 19 分钟前/同步对象 820）+「立即同步」按钮。"数据是几分前的"永远可见——比我们的 sync_stalled 告警更日常化 |
| 9 | **账户域信息密度** | 状态徽标下带**禁用原因副行**（实测"已停用·违反广告诚信政策"）+ 花费上限/已花费/剩余三元组 + 备注/发布身份字段 |
| 10 | **追踪参数通用插值** | `{{campaign.name}}/{{adset.name}}/{{ad.id}}` 多账户各自取值。升级我们的子码占位符体系（⚠️ ad.id 占位符曾有像素不 fire 坑，需带校验） |
| 11 | **通用查询列（子码徽标同构）** | 广告行直接展示 `[子码:xxx]` 徽标——与我们子码体系同构，确认方向正确；15 指标列全可排序 + 用户可配列 |

---

## P2 观察（按需）

- **「需重新授权」首页红色一级卡** —— 对我们 FB 重授权阻塞期尤其对症，账户/令牌失效应升为首页最高优先级卡
- **BM「拥有 vs 第三方共享」归属语义** —— agency 借号跑量场景（实测该 BM 15 账户全为第三方共享）；我们账户体系缺"归属"层
- **主页池 = persona 登记池**（分类标签：女装/男装/保健美容 + 粉丝数，纯登记无操作）
- **表格延迟加载 + 列管理**（隐藏列可开）、**页面留痕**（操作日志含单个改名）

---

## 明确不跟进

| 项 | 原因 |
|---|---|
| facebook-profiles 个人号池 | 合规风险（号池授权台账），但其「授权到期倒计时+重授权」模式可平移到 FB 长期令牌管理 |
| 商业化对标 | 对方零商业化（无定价/无营销页/注册无套餐）——我们 SaaS 路线全面领先，无需对标 |
| TG 频道/关键词监控 | 对方根本没做（telegram-watches 实为账户告警白名单）——若我们做 TG 命令查询/紧急暂停是**超越点**而非追平 |

---

## 我们已领先的代差点（守住）

双平台 FB+TT · AI 文案/受众 · 素材数据回流 · 落地页全链路（子码/防护/自检/日志）· 多租户 RLS · 官网+自助注册 · 守护引擎（止损/扩量/学习期/风暴上限）· KPI 目标感知转化映射（对方无转化概念）· i18n 中英。

---

## 数据规模参考（该账号实测）

同步对象 820 · 广告账户 20 · Campaign 43 / Ad Set 157 / Ad 155 · 操作日志 151 条 · BM 下 15 账户（全第三方共享）。

---

## 落地进度（2026-09-05 批F，commit 4d58233/a573544）

| 对标项 | 状态 |
|---|---|
| P0① 按素材批量生成系列 | ✅ 已上线（DeployIn.asset_ids；模板=母版每素材一完整系列，系列名=素材名；FB+TT 双链；partial 一等状态+重试） |
| P1⑦ 账户分组 | ✅ 已上线（accounts.group_label 迁移0084；Ads 分组列/筛选/批量设；Dashboard 分组 chip） |
| P1⑨ 禁用原因副行 | ✅ 已上线（disable_reason 迁移0084+account_sync 落库；useStatus 全枚举；Ads 状态副行+Dashboard 可用明细） |
| P1⑩ 追踪参数通用插值 | ✅ 已上线（7 静态白名单占位符逐账户解值+URL编码；{{ad.id}} 显式拒绝防像素不 fire 复发） |
| TG④ 通知白名单矩阵 | ✅ 已上线（简化版：级别开关 warning/info，critical 恒推；user_tg_bindings.prefs 迁移0085；账户维度过滤预留 act_id→target 列） |
| P0⑤ 花费骤变告警参数 | ✅ 批R已上线（7天均值×3倍+$50 下限） |
| 未做（保持不做清单） | 草稿树/同步灯日常化/列管理/BM归属/persona池/授权倒计时——见上「明确不跟进」与 P1/P2 表 |

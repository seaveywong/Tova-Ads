# 广告管理器本轮收尾 — 2026-09-09

> **状态：开发中，已保存代码，未部署，未达到 HANDOFF 的整批完成定义。**
> 用户要求「收尾一下，然后补回文档，额度不够了」，因此暂停继续开发和上线。
> 下一位先读本文，再结合 `PROMPT_广告管理器重构.md`、`交接_广告管理器现状.md`；不要把旧现状当成当前本地代码，也不要把本地代码当成生产版本。

## 本轮基线与线上操作

- 起点提交 `daae3d7`，分支 main。
- 重新按优先级读完六份广告管理器文档/源码；此前四份基础交接也已读。
- 生产 `/health` 返回 200、status=ok、version=1.3.5。
- 上传并运行 `_smoke_batch_f.py`。第一次因服务器缺少 `_smoke_kpi_ai.py` 中断；补齐该测试脚本后，完整重跑得到 **SMOKE_RESULT: ALL_PASS**，其中 KPI 的 11 项回归全部通过。
- 用临时 `_admanager_baseline.py` 包装基线，finally 恢复原账户分组和 TG warning/info 偏好。两次运行都输出 RESTORED。巡检反馈 evaluated=3、hits=0、paused=0、scaled=0。
- 仅上传了测试脚本；**未上传应用代码、未重启服务、未部署 Cloudflare、未执行真实预算编辑/手动暂停/删除广告**。
- 临时基线包装脚本在仓库外 `D:/dev/Mira_One/_admanager_baseline.py`，服务器 `/opt/toveads/backend/_admanager_baseline.py`。不含明文凭据。

## 已写入本地代码

| 文件 | 改动 |
|---|---|
| `frontend/src/views/AdManager.vue` | 合并三个重复表格；可选指标列；逐层 localStorage 保存列/排序；面包屑；搜索包含账户及父层名称；显示 FB 成效、综合转化双列；快照及超过24小时过期标注；日/总预算统一行内、输入为空；预算未核验不再提示成功；批量失败/未核验汇总及失败选择保留；缩略图逐级回退；修复越出 style 标签的样式；移除多处彩色装饰 emoji |
| `frontend/src/composables/adManagerView.js` | 账户+平台+实体复合键，父层查找，搜索，多币种排序，缺失成效处理，配置净化；解决原来第二次排序覆盖脱管置底的问题 |
| `frontend/src/locales/views/admanager.js`、zh/en 入口 | 新增18键×2语言；36条均通过实际 message compiler；新英文无CJK |
| `backend/app/routers/ads.py` | 聚合并返回 results_fb、综合转化、单次成效费用；按 platform+act_id+ad_id 分组及向父层汇总；新增每行 snapshot_at、metrics_updated_at；结构实际拉取时间存入已有JSON，不改 schema |
| `backend/app/services/guard_engine.py` | **仅1行**：更新已有 PerfSnapshot 时同时更新 updated_at；未重写暂停/预算执行函数 |
| `_smoke_ad_manager.py` | 2项无外部写操作的数据断言测试：相同ID跨账户/平台隔离、FB与综合转化口径、零值、缺失/不完整数据 |
| `frontend/src/composables/adManagerView.test.js` | 4项Node测试：跨层身份与搜索、脱管置底及多币种排序、缺失成效不回退综合转化、配置净化 |

## 已完成验证（不等于上线验证）

- `node --test frontend/src/composables/adManagerView.test.js`：4/4 PASS。
- 隔离 Python venv 执行 `_smoke_ad_manager.py`：2/2 PASS，使用虚拟DB地址和测试Fernet key，无生产数据写入。
- `python -m py_compile backend/app/routers/ads.py backend/app/services/guard_engine.py _smoke_ad_manager.py`：PASS。
- `npm --prefix frontend run build`：PASS，现有 VueUse annotation / chunk size 警告仍在。
- `rg -l -g 'AdManager-*.js' 'manager-table|admanager-view-v1' frontend/dist/assets`：命中本轮 `AdManager-TIaGBDbP.js`。下一次build文件名会变，必须重新验证。
- 新 locale fragment 的36条消息用 `@intlify/message-compiler` 实际编译：PASS。
- `_scan_i18n.cjs`：zh/en各2920键，差集0；**仍报5项存量问题，不能声称全库语言门通过**：zh/en 的 launch.urlPhHint、landing.copyFbLink 各1项花括号扫描告警（可能是合法转义，需要实际编译核实），en 的 layout.langToZh 含中文。
- `git diff --check`：通过（仅Windows换行提示）。
- **未做浏览器交互/窄屏验收、未做新版线上 `/ads/list` 与数据库对账、未做部署前备份/源码一致性/双门/restart/health/新版smoke/journal检查。**

## 下一位必须先处理的缺口

1. 用浏览器验收当前合并表格：三个层级、列勾选及刷新记忆、中英文、窄屏横向滚动、潜客Tab、创意预览、批量失败保留和预算未核验反馈。构建不能代替运行时验收。三个并行助手均因额度中断，没有产出其他代码。
2. 核实历史 `results_fb`：0093迁移给历史行填0，本轮按数据库现存字段汇总，**尚不能区分迁移默认0与真实采集0**。缺行/NULL不回退综合转化，但历史0的来源问题仍需解决或明确标注。不要编造历史数据。
3. 系列/组以前没有独立真实拉取时间，而且 guard 会刷新整行 updated_at。因此旧JSON显示「快照时间未知」，下一次真实结构同步才带 snapshot_at。不要用整行更新时间伪装结构新鲜度。当前15分钟/5分钟两个同步周期仍保留。
4. 搜索已包含父层和账户名称，但仍服从当前下钻及账户过滤；全局跨父层搜索/退出下钻的体验尚需验收调整。平台/账户切换时旧面包屑及同ID父层消歧也需要补齐。
5. 批量前端虽有明细及失败保留，后端逐项异常隔离、每条统一回传act_id、verified统计尚未完善；全部选中含不可操作行时确认数量与实际提交数量需核对。不能把部分成功当全成功。
6. 账户状态3被旧代码当被禁，禁用原因语义和平台限定账户查找尚未统一；检查列表、下拉菜单及单/批操作的同一套前置校验。保留资金执行函数。
7. Live Verify仅核验广告层；本轮只补了按act_id限定本地patch，系列/组不能声称实时核验成功。跨worker refresh-status仍是旧进程内状态，尚未修复。
8. 新版预览仍需测损坏图片、视频文案/CTA、无图创意；拒登理由入口、子码/转化口径提示等旧功能需回归，避免合并表格漏入口。
9. **Breakdowns、行内创建精确实体范围规则均未实施。** 16项对标不能标记全部完成，不能宣称1:1 FB已经达成。
10. 处理全库i18n门检，检查剩余彩色emoji及文案语义；预算未核验目前复用 fakePauseWarn，需要换成预算专用双语提示。没有修改1.0或schema。
11. 本轮所有检查通过后，再按照 HANDOFF 原流程部署。先确认线上与基线的源码差异，做代码/DB备份，commit后上传，compile+import双门后restart、health，再断言新版API真实数据及journal、push、更新TECH_REVIEW。**不要直接拿当前dist绕过验收上线。**

## 恢复工作用命令

```powershell
cd D:\dev\Mira_One\toveads
git status --short
node --test frontend/src/composables/adManagerView.test.js
python -m py_compile backend/app/routers/ads.py backend/app/services/guard_engine.py _smoke_ad_manager.py
npm --prefix frontend run build
rg -l -g 'AdManager-*.js' 'manager-table|admanager-view-v1' frontend/dist/assets
```

本地后端测试 venv：`$env:TEMP/toveads-kpi-ai-venv/Scripts/python.exe`。需要设置仅测试用 DATABASE_URL、DATABASE_SUPER_URL、JWT_SECRET、合法32字节Fernet的 FB_CRED_KEY、APP_ENV=development。勿把生产凭据打印进日志或提交Git。


## 给后续 AI 的最新建议与目标补充

用户在本轮收尾后再次明确：**把 FB 广告管理器 1:1 复刻过来，并且成功**。最新完整指令见 [PROMPT_广告管理器重构.md](PROMPT_广告管理器重构.md)，应按该目标继续实施，而不是只修本轮列出的P1或只完成静态表格。代码检查点为 **f8dc294（已推送、未部署）**。

建议先用实际浏览器审查已有改动，不要继续盲写大块模板。建立含参考证据和验收证据的FB对标矩阵；16项是最低覆盖范围，当前实际FB核心行为中发现的差距也要补齐。优先解决数据真实性、身份隔离、遗留回归，再接真实细分与精确实体规则，最后视觉逐项对照和部署验证。

重点风险：历史0值不能冒充测得0；FB成效与综合转化不能混用；reach去重及频次不能盲目跨广告相加后声称与FB一致；不同优化目标的成效汇总也要核实可比性。多币种、日期/账户时区、归因窗口等比较条件必须一致，无法一致的要明确说明。

规则接入要同时覆盖保存、编辑、诊断、执行的范围过滤，防止无schema新增字段被编辑器丢弃，导致规则扩大到整账户。实时核验不能拿广告层结果为系列/组背书。资金操作全部保留既有执行函数，使用明确测试对象或mock验证，不拿生产投放做试验。

完成标准是真实交互+真实数据+成功上线+上线后验证，不是代码提交、build或HTTP200。如因外部权限/API能力仍有差距，须带证据列为未完成；不要伪造对标或成功。该追加只更新建议和提示词，没有继续改应用代码或部署。


## 创建与编辑专项实施规格

用户追加要求已整理为 [实施规格_FB广告创建与编辑_配置及API映射.md](实施规格_FB广告创建与编辑_配置及API映射.md)：含完整层级、配置说明、字段依赖、Meta API映射、创建/编辑差异、API条件限制及执行提示词。下一位实现创建编辑时先读此文；本次只新增文档，未做平台写入或部署。

# Tova Ads — 产品功能手册

> 版本 1.3.5 · 2026-09-02 · 双平台（Facebook + TikTok）多租户广告管理 SaaS
> 本文档面向运营/客户，覆盖全部已上线功能。技术细节见 TECH_REVIEW.md。

---

## 快速开始

1. **连接令牌**：令牌管理 → Facebook：从 App 卡片点「连接」授权 → TikTok：从 App 卡片点「连接」（需超管先配置 TikTok App）
2. **导入账户**：令牌管理 → 载入账户 → 勾选要纳管的广告账户
3. **上传素材**：素材库 → 上传图片/视频 → AI 分析生成文案和受众
4. **创建投放模板**：投放模板 → 新建模板（选平台 FB/TT）→ 选素材/受众/预算/落地页
5. **批量部署**：模板卡片 → 部署 → 勾选目标账户 → 开始部署

---

## 模块总览

| 模块 | 功能 | 页面入口 |
|---|---|---|
| 数据看板 | KPI/趋势/账户明细/守护概览/告警 | Dashboard |
| 广告账户 | 账户列表（余额/状态/消耗/绑令牌）+ 批量操作 | Ads |
| 广告管理器 | 三层浏览（系列→组→广告）+ 暂停/预算/改名/删除/诊断 | AdManager |
| 投放模板 | 模板CRUD + 批量部署 + 进度/重试 + 已部署清单 | LaunchTemplates |
| 表单模板 | Instant Form 编辑器 + AI 生成 + Messenger 消息 | FormTemplates |
| 素材库 | 图片/视频上传 + AI 文案/受众分析 + TK 规格标注 | Assets |
| 落地页 | 页面CRUD + 像素/域名 + 子码 + 防护 + 自检 + 日志 | Landing |
| 守护引擎 | 规则CRUD（止损/成本/扩量）+ 巡检 + 哨兵 + 紧急暂停 | Guard |
| 令牌管理 | FB/TT 令牌生命周期 + 载入账户 + 数据健康 | Tokens |
| 系统设置 | 调度/告警/AI/CF/邮箱转发/Webhook/保留/汇率/TG | Settings |
| 团队管理 | 成员/角色/权限 | Members |
| 日志中心 | 操作/系统/登录日志 + 全链路追踪 | AuditLog |
| 潜客（CRM） | 表单提交列表 + 状态流转（新/联系/成交/流失） | AdManager →潜客 Tab |

---

## 一、数据看板

### 双 Tab
- **数据看板**：KPI 核心卡（消耗/转化/CPA/ROAS，点击切换明细视角）+ 全宽趋势图 + 账户明细表 + 守护概览/告警
- **落地页数据**：访问/通过/屏蔽统计 + 落地页明细 + 趋势

### 筛选
- **平台**：全部 / Facebook / TikTok（全站全局切换——顶部平台上下文条，仅数据页显示）
- **日期**：今日 / 昨日 / 近2天 / 近7天 / 近30天 / 自定义
- **转化分类**：购物/消息/潜客/互动/流量
- **账户多选**：下拉可搜索，选项带平台色 chip

### 告警面板
- 按平台隔离（切到 TikTok 只看 TT 告警）
- 级别标记（critical红/warning黄/info蓝），点击查看详情
- 一键标记已读 / 全部已读

### KPI 卡
| 指标 | 含义 | 点击 |
|---|---|---|
| 消耗 | 全账户 USD 合计（可切本币） | 明细按消耗排序 |
| 转化 | FB 转化数合计 | 明细按转化排序 |
| CPA | 消耗/转化 | 明细按 CPA 降序 |
| ROAS | 转化价值/消耗 | 明细按 ROAS 升序 |
| 余额 | 可用额度（limited 账户） | 切余额视角，可勾选复制 |

### 账户明细表
- 五视角 Tab：消耗/转化/CPA/ROAS/余额
- 默认只显示纳管账户；「含已移除」开关可看历史
- 消耗/CPA 双币种列（本币 + USD）
- 行点击跳转广告管理器（余额视角为勾选复制）
- 平台 chip、已移除灰显、异常标记

---

## 二、广告账户

- 账户列表：余额/可用额度/状态/绑令牌/近3天消耗
- **平台分段**：全部模式按 FB/TT 分段显示（品牌色段头+计数）
- **搜索**：按名称/ID 模糊搜索
- **批量操作**：全选/移除纳管/批量同步
- **载入账户**：勾选令牌能管但未纳管的账户导入（FB/TikTok 双源）
- 移除纳管：确认弹窗显示在投广告数，提示「广告不会停、止损失效」

---

## 三、广告管理器

### 三层浏览
系列 → 广告组 → 广告（面包屑导航，点击下钻自动锁定账户）

### 读
- 状态筛选：全部/生效/已停/异常（拒审/PENDING 等）
- 列：消耗（本币+USD）/转化/CPA/ROAS/预算/状态/素材缩略图/子码
- 数据更新时间显示（超1h 变橙）
- 拒审广告 ⚠ 弹窗显示 FB review_feedback 详情

### 写
- 暂停/恢复（单个+批量）
- 日预算编辑（点预算单元格，×1.2/×1.5/×2 快捷倍率）
- 改名（三层）
- 删除
- 跳转链接管理（单条+全局+角标计数）
- 诊断面板（规则诊断+消耗趋势）

---

## 四、投放模板

### 模板
- CRUD + 复制变体 + 归档
- **平台**：FB / TT（建时选，不可改；编辑器只读 chip）
- FB 模板：目标/预算/受众/版位/披露/频次/归因/Dayparting/主页/像素/落地页/子码/文案/表单/消息模板
- TT 模板：目标/预算/受众/版位（TT 像素 Code + 落地页/子码）

### 部署
- 部署抽屉：全选/只选正常账户/清空 + 实时预算合计
- 每账户选主页/像素（FB）/ TT 像素 Code（TT）
- 进度弹窗：逐账户状态（创建中/成功/失败+原因）+ 失败重试
- 部署完成后自动刷新广告缓存

### 已部署清单
- 模板卡片 →「已部署 N」→ 抽屉显示每次部署的账户/广告/当前状态

### 预检（不花钱）
- 部署前检查 payload：预算换算/子码存在/像素/汇率/域名
- 子码不存在 → 警告（部署将丢失子码追踪）

---

## 五、表单模板

- **双平台**：新建 → 选平台弹窗（FB 蓝点 / TT 渐变粉点）
- Instant Form 编辑器：标题/隐私政策/联系字段/自定义问题/感谢页
- AI 生成：从素材 AI 文案生成表单问题
- 预览 mockup
- **投放模板集成**：FB 投放模板 → 表单下拉只列 FB 表单（TT 同理）

---

## 六、素材库

### 上传
- 图片（jpg/png/webp）+ 视频（mp4/mov）≤200MB
- **去重**：同内容重复上传 → 409 提示已存在
- 批量上传进度条（n/m + 成功/失败/重复实时计数）
- 名称/标签/国家/语言 可编辑

### AI 分析
- 双模型：DeepSeek 文案 + Gemini 视觉（图片看图/视频抽帧）
- 生成：分析摘要 / 标题×N / 正文×N / 兴趣词 / 受众建议
- 批量分析：只问一次投放目的
- 状态角标：旋转蓝环=分析中 / 绿✓=完成 / 红✕=失败

### 展示
- 卡片：缩略图+角标（AI 状态/TK 9:16 竖屏）+ 名称+标签+国家
- 详情弹窗：大图 + AI 全量结果 + 快捷操作（分析/编辑）

---

## 七、落地页

### 页面管理
- CRUD + 发布到 CF Worker + 自定义域名/子域名
- 四分区编辑：基础/域名/像素/防护

### 像素
- FB 像素（多像素）+ TT 像素（Code + Events API Token + test_event_code）
- 建 TT 像素自动发测试事件点亮（约2h后可选优化目标）
- FB CAPI 服务器端双发（实验性，默认关，需先 Test Events 验证）

### 子码
- 生成/编辑/归档/硬删 + 回收站
- 子码链接带广告参数 → 访问自动绑定 ad_id
- 状态：reserved → active（自动）/ archived

### 防护
- 爬虫拦截（TK 索引/FB 审核等 UA）
- 预览模式（?_pv=64位 token 跳过防护）
- IP 去重 + 时间窗防重复访客

### 自检
- 9 项矩阵：URL/DNS/Worker/像素/目标/防护/FB屏蔽/子码/预览
- 发布后自动跑 + 手动触发 + 告警

### 日志
- 访问日志：子码/来源（受控/外部/爬虫/机房）/平台/fire 像素 ID
- 筛选：页面/账户/事件/结果/搜索
- 统计 chip：来源分布（点选即筛）

---

## 八、守护引擎

### 规则
- 类型：空耗止损（bleed_abs）/成本超标（cpa_exceed）/效果下滑（trend_drop）/瞬烧制止（budget_burn_fast）/智能扩量（slow_scale/roas_scale）
- 动作：只告警（observe）/暂停广告/暂停组/暂停系列/加预算（scale）
- 参数化：每个参数带默认值提示（不预填）
- 分类筛选 chip：全部/止损/成本/下滑/扩量

### 巡检
- 并发 1-8（默认 4）；每 5 分钟一轮
- 学习期保护：新广告 24h 内不动（可调 0-720h）
- 紧急暂停：全部账户一键停（含 TT）+ 回读核验

### 告警
- 平台隔离（FB 告警只看 FB，TT 只看 TT）
- 风暴上限：每租户每类型每日 30 条（0=不限）
- TG 推送（critical/warning 自动发）+ 站内信
- 令牌过期/账户异常/覆盖丢失/预算进度 50/75/90/98%

---

## 九、令牌管理

### FB 分区
- 令牌列表（状态/关联账户数/权限快照/可用性）
- App 卡片发起 OAuth 授权 + 复制授权链接
- 载入账户 / 全部刷新 / 数据健康（脏数据诊断+清理）
- 操作：改名/更新密钥/删除（解绑不删账户）

### TT 分区
- App 配置（超管）→ 授权连接
- 24h 自动刷新（倒计时显示）+ 手动立即刷新
- refresh_token 寿命（<30天 红标+重新授权按钮）
- 载入账户（勾选授权过的 advertiser）

---

## 十、系统设置（超管）

| Tab | 功能 |
|---|---|
| 账户 | 用户名/密码修改 |
| 时区 | 显示时区选择 |
| 任务调度 | 巡检/哨兵/watchdog 等频率调整 |
| 巡检与告警 | 并发数/学习期/风暴上限三旋钮 |
| AI 配置 | 文案（DeepSeek）+ 视觉（Gemini）双模型 |
| 域名服务 | CF Token/账户 ID/邮箱管理 Token |
| 邮箱转发 | CF Email Routing：启用/目的地/别名映射 |
| FB Webhook | 潜客实时回调 |
| 数据保留 | 快照/日志保留天数 |
| 汇率 | 币种汇率（止损换算） |
| Telegram | TG 绑定/通知测试 |
| 保活保护 | 防休眠设置 |

---

## 十一、多租户/RBAC

- 团队切换（topbar）+ 团队管理（设置→团队）
- 角色：Owner / Operator / Finance / 超管 / 自定义角色
- 数据隔离：RLS（Row Level Security）数据库级
- 令牌/账户/模板/素材 全部按租户隔离

---

## 十二、平台切换

### 全局上下文条
- 位置：顶栏正下方（sticky，滚动可见）
- 仅在数据页显示：仪表盘/广告账户/广告管理器/落地页
- 品牌色语境：FB=蓝色顶线 / TT=粉青渐变顶线

### 各页响应
| 页 | 响应 |
|---|---|
| 仪表盘 | KPI/趋势/明细/告警全隔离 |
| 广告账户 | 列表按平台过滤/分段 |
| 广告管理器 | 三层列表+账户下拉过滤 |
| 落地页日志 | 账户下拉预筛（行不过滤——落地页跨平台） |

---

## API 概览（258 端点）

| 路由组 | 前缀 | 功能 |
|---|---|---|
| auth | /auth | 登录/注册/团队切换/token 刷新 |
| fb | /fb | 令牌/账户/载入/import/数据健康 |
| tt | /tt | TT OAuth/凭证/载入/App 配置 |
| ads | /ads | 广告管理器（三层/写操作/诊断/缓存） |
| launch-templates | /launch-templates | 投放模板 CRUD/部署/重试/预检/清单 |
| form-templates | /form-templates | 表单模板 CRUD/AI 生成/部署 |
| dashboard | /dashboard | 看板汇总/趋势/导出 |
| guard | /guard | 规则/巡检/哨兵/紧急暂停 |
| landing | /landing | 落地页 CRUD/发布/自检/日志 |
| landing-lib | /landing-lib | 像素/域名 |
| assets | /assets | 素材上传/AI 分析/去重/孤儿清理 |
| notify | /notifications | 通知列表/已读（平台过滤） |
| settings | /settings | 系统配置全量 |
| rbac | /rbac | 角色/权限 |
| admin | /admin | 平台管理（超管） |

---

## 环境依赖

| 依赖 | 用途 |
|---|---|
| FastAPI + PostgreSQL (RLS) | 后端 |
| CF Pages | 前端部署 |
| CF Workers | 落地页 |
| CF Email Routing | 邮箱转发 |
| DeepSeek API | AI 文案 |
| Gemini API | AI 视觉 |
| Telegram Bot | TG 通知 |

---

## 已知限制

| 限制 | 说明 | 解决路径 |
|---|---|---|
| FB BM 受限 | 当前无法建/投 FB 广告 | 申诉中（已部分恢复） |
| FB read_insights 未获批 | Page 级数据不可用 | ads_read 已够核心功能 |
| TT sandbox 未实测 | TT 全链路待校准 | 等开发者注册 + app_id |
| TT 表单部署 | UI 已隐藏（部署链路待接） | sandbox 后接线 |
| Spark Ads | 决策跳过 | Phase 2 |
| Advanced Matching | 决策跳过 | Phase 2 |

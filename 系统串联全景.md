# ToveAds 系统串联全景 — 从零到广告在投、数据回流、自动止损

> 版本 2026-09-08 · 站在**运营者视角**，把全部底层功能怎么串联起来用讲清楚。
> 事实来源：`backend/app/` 代码逐文件核实（含 2026-09-08 上线的三层树模板，迁移 0088）+ `TECH_REVIEW.md` 各批次记录。与 `ARCHITECTURE.md`（架构地图，2026-09-05）配套读：那篇讲"系统长什么样"，本篇讲"怎么一环扣一环跑起来"。
> 每章末尾**串联要点** = 这条链上最容易断的点 / 依赖的前置。

---

## 全链一图（0 → 在投 → 回流 → 止损）

```
①接入                ②资产准备                    ③模板与部署
FB App/OAuth ──▶ 令牌(operate/manage) ──▶ 载入/导入账户(200单批/令牌槽位) ──▶ 分组
                        │
                        ▼
素材库(AI文案) ─▶ 表单模板 ─▶ 消息模板 ─▶ 落地页(发布/防护/自检) ─▶ 像素库 ─▶ 受众库
                        │
                        ▼
三层树模板(系列/组/广告+素材组展开) ──▶ 预检(不花钱) ──▶ 部署抽屉(主页/像素策略) ──▶ runner 建 FB 对象
                        │                                              │
                        │                                      /a/slug?ad={ad.id}
                        ▼                                              ▼
④运行期 ◀──────────────────────── 子码归因链(点击→worker→像素fire→事件回流) ─────┘
  巡检(5min insights∩ACTIVE) ─▶ 规则评估(10类,学习期默认0) ─▶ 止损(升级链+回读核验)
  哨兵(3min kill-switch) · 预算告警(15min) · watchdog(10min) ──▶ 通知(站内+TG,dedup+风暴cap)
  perf_snapshots(巡检独家写入) ──▶ 看板(北京业务日) · 广告管理器(ads_cache) · 潜客CRM
⑤支撑：RLS多租户 · RBAC · i18n · 审计trace · 汇率(每日) · 备份 —— 贯穿以上全部
```

---

## 一、接入：把账户接进来（Tokens 页 + Ads 页）

### 1.1 操作面
1. **登记 FB App**（超管，设置或 Tokens 页 App 卡片）：填 app_id/app_secret，标记"系统 App"（全租户共享）或租户自有。
2. **OAuth 授权个号**：Tokens 页 → App 卡片「连接」→ 跳 FB 授权页 → 同意后回调自动完成，令牌落库。
3. **载入账户**：Tokens 页「载入账户」→ 勾选令牌能管但未纳管的账户 → 导入。
4. **TikTok**（可选）：超管配 TT App → 授权连接 → 载入 advertiser。
5. **分组**：Ads 页勾选账户 → 批量设置 group_label（≤50 字符纯文本）。

### 1.2 底层发生了什么
- **App 表** `fb_apps`：`tenant_id=NULL` 即系统级（全租户共享）；`access_level` dev/standard 决定创意路径——dev 走跟帖 `object_story_id`，standard 走内嵌 `object_story_spec`（`models/fb_app.py`）。注意 access_level 目前不走 API 编辑，是半手工字段。
- **OAuth 全流程**（`routers/fb_oauth.py`）：`GET /fb/oauth/start` 生成 HMAC 签名 state（TTL 30 分钟）→ FB dialog（scopes = `ads_management, ads_read, business_management, pages_show_list, pages_manage_ads, pages_read_engagement`）→ 回调 code 换 short-lived → `fb_exchange_token` 换 long-lived（约 60 天）→ `/me` 拿个号 → `debug_token` 把权限快照存 `permission_snapshot` → upsert `fb_credentials`（token_type=operate，加密列 `access_token_enc`）→ 顺手跑孤儿账户重绑 `reassociate_orphan_accounts`。
  - **关键限制**：`read_insights / pages_manage_posts / pages_manage_metadata` 已从 scopes 摘除（advanced 权限放 OAuth 会 Invalid Scopes，须 App Review 过审后加回）——**过审前建帖与 webhook 订阅整条不可用**。
- **令牌分工**（`core/fb_tokens.py`，多令牌体系的芯）：
  - `token_type`：**operate=写** / **manage=读+暂停兜底** / user=只读；
  - 账户↔令牌多对多表 `account_fb_credentials`（priority 数字越小越高）构成候选池；
  - **读 = RR 轮换**分摊调用；**写/暂停 = 绑死 priority 最高**那只（防 FB 侧孤儿对象），同优先级 operate 排 manage 前；
  - 权限判定以 `permission_snapshot.scopes` 含 `ads_management` 为准；
  - 限流（code 17）→ status=rate_limited + **30 分钟 cooldown 出池**，过期自动回池；
  - **FB 长令牌无自动续期**（只有 TT 有刷新 cron）——靠 watchdog `debug_token` 检测 + 人工重授权。
- **载入/导入**（`routers/fb.py`）：
  - `GET /fb/credentials/loadable-accounts`：最多 4 线程并发拉各令牌 `/me/adaccounts`（light 字段），按 account_id 合并去重、带 tokens[] 覆盖标记，**5 分钟缓存**；「已导入」= accounts 表且 `is_managed=true`。
  - `POST /fb/import`：**单批上限 200**（超出 400，分批即可）；真正持续卡人的是**每令牌 `max_accounts` 槽位**（operate 新令牌默认 100，来自系统设置 import_default_cap，0=不限）——满额令牌的账户进 `skipped_over_limit` 明细**静默返回不报错**；写入 `accounts`（`is_managed=true`）+ `account_fb_credentials`（priority=0），余额等重字段由后台任务补全；重导已移除账户 = 恢复纳管（软删语义）。
  - **铁律**：TT OAuth 拉到的 advertiser 一律 `is_managed=false`——令牌能管≠纳管，只有显式导入才纳管。
- **账户状态同步** `run_account_status_sync`（30min，锁 110）：每令牌翻页拉 `account_status / disable_reason / balance / spend_cap / amount_spent` 只更新 managed 账户；异常集合 {2,3,7,9,100,101} 发 critical（一次事件只告一次），恢复发 info；低额告警默认 $20（可关）；**顺带同步该账户的 FB 像素进像素库**（source=sync）。
- **TT 侧**：access 约 24h / refresh 约 365d，6h cron 刷新（`run_tt_token_refresh`，锁 114）——**TT 刷新即轮换**（每次返回全新 refresh_token，必须一次原子写回）；refresh 剩 <30 天红标告警重新授权。
- **分组**：`PUT /fb/accounts/group` 批量写 `accounts.group_label`（纯文本标签，空=NULL），写 action_logs 留痕；Ads 页按它做筛选、同组排序、批量改。

### 1.3 与上下游的衔接
令牌候选池（account_fb_credentials）是**全系统一切 FB 读写的入口**：巡检拉 insights、部署建广告、哨兵停系列、看板同步、落地页像素同步——全部经 `cred_for_account_op / client_for_account_op` 选令牌，并按 `accounts.platform` 分发到 FB 或 TT 客户端。`is_managed` 软删语义贯穿：所有"当前操作"只算 managed=true，历史快照保留全部。

**串联要点**：接入链的第一断点是 **OAuth scopes 被削**（过审前跟帖建帖/leads webhook 不可用）和 **FB 长令牌无自动续期**（失效后该令牌覆盖的账户读写全断，只等 watchdog 告警+人工重授权）；第二断点是 max_accounts 满额静默 skipped——批量导入时必须看明细。

---

## 二、资产准备：素材 → 表单 → 消息 → 落地页 → 像素 → 受众（Assets / FormTemplates / Landing 页）

### 2.1 素材库（Assets 页）
- **操作**：上传图片/视频（≤200MB）→ 设名称/标签/国家/语言 → 点 AI 分析 → 得文案与受众建议。
- **底层**：文件存服务器 `/opt/toveads/assets`（`storage_key`=uuid），公开 URL `api.tovaads.com/static-assets/{key}`；去重=同租户按 file_size 缩小候选后 md5 比对，命中 **409 拒绝**。AI 分析双通道：文本模型出文案 + 视觉模型看图（**视频用 ffmpeg 抽 10%/50%/90% 三帧**）；产出落 `assets.ai_copy_json`（analysis/headlines/bodies，headline 截 40 字符=FB 硬上限）与 `ai_audience_json`（interests/countries），支持手动编辑。FB 部署用的 `fb_image_hashes` 按**账户**缓存（像素 hash 不能跨账户复用），行锁合并防并发丢条目。
- **坑**：默认配置两个模型其实都是 Gemini 2.5 Flash（DeepSeek 是可切换项）；ffmpeg 缺失→视频分析直接失败；视觉 key 未配→按钮 400。

### 2.2 表单模板 + 消息模板（FormTemplates 页）
- **操作**：新建（选平台 FB/TT，建后不可改）→ 1:1 FB Instant Form 编辑器（左侧手机壳实时预览/右侧分区）→ AI 从素材文案生成表单问题 → 需要时另建 Messenger/WhatsApp 消息模板（开场白+快捷回复）。
- **底层**：`lead_form_templates.config_json` 存完整配置，`config_hash` 变更 → 缓存的 `fb_form_id` 置空、下次部署**重建** FB 表单；部署 = 真调 FB API `POST {page_id}/leadgen_forms`（TT 走 `lead/form/create/`）。消息模板 `message_templates.type = messenger|whatsapp`：**Messenger 部署时真传创意**（welcome_text/ice_breakers），**WhatsApp 目前仅本地存储+表单感谢页预览**，不进 FB payload（FB 侧字段后续接入，属显式半接线非假实现）。
- **衔接**：投放模板/树广告节点按 objective 绑定表单+消息；部署时若 LEADS 目标没绑表单，runner 会用素材 AI 文案**现场自动生成并创建表单**（静默兜底）。

### 2.3 落地页（Landing 页；后端在 `routers/landing.py`，注意不是 launch.py）
- **操作**：新建页 → 四分区配置（基础/域名/像素/防护）→ 发布 → 看自检 9 项矩阵 → 需要时生成子码。
- **底层**：
  - 发布 = 经 CF API 用 wrangler 把 Worker 部署到项目 `tovaads-landing-{id}`，页面配置以 `"const LP_CONFIG = {json}"` **前置注入** Worker 源码；发布前 node `--check` 语法门 + dry-run 运行时门，发布后 curl 线上 `/a/`，非 200/302 **自动回滚上个备份**并 critical 告警。
  - 双模式：**redirect** = Worker 直接 302 到目标；**display** = Worker 调后端 `route_next` 拿像素+目标，编码成 `_d` base64 参数 302 回落地页，页面 JS 解码后 fire 像素。
  - 子域名：每页独立 `lp{id}.根域`（可自定义前缀），`bind_custom_domain` 绑定时**自动补 CNAME（proxied）→ pages.dev**——这是修"域名验证永远 pending、SSL 颁不下来"的关键联动。
  - 防护：8 类规则（国家/设备/UA/referer/query/required_query/机房 ASN 等）+ 频控 + IP 去重 + 内置爬虫 UA 黑名单；预览模式 `?_pv=64位token` 跳过全部防护。
  - 自检 9 项（发布后自动跑：发布状态/公开链接/DNS+SSL/Worker 存活/像素/跳转目标/防护规则数/**FB 封禁探测**/预览模式），另有每小时屏蔽扫描 cron。
  - 域名白名单：发布用的每个根域必须在本租户 `landing_domains` 里 active，否则 400（防跨租户接管）；V1 租户对域名池只读，超管分配。

### 2.4 像素库（Landing 页像素分区 / landing-lib）
- `landing_pixels` 表：`pixel_id` 明文（像素本就暴露在页面 HTML）+ `platform` fb/tt + `tt_access_token_enc`（Events API token，加密）+ `test_event_code` + `fb_capi_enabled`（**FB CAPI S2S 灰度开关，默认关**）+ `act_id` 绑账户。
- FB 像素两条来源：手动建 / account_sync cron 从 FB API 自动同步（source=sync）。TT 像素建时 token+test_event_code 齐备会**立即自动发一条测试事件**点亮像素（约 2h 后可选优化目标）。
- 页级 pixel_ids 注入 LP_CONFIG 只是**兜底**；display 模式运行时像素优先级 = **广告组绑定像素（ads_cache 反查）> LandingPixel by act_id > 页级配置**。

### 2.5 受众库（SavedAudiences）
- `saved_audiences`：interests_json（FB 兴趣词真 ID，经 `/audiences/search` 代理 FB adinterest 搜索选择）+ countries + 年龄 18-65 + 性别 + 策略（broad_interest/broad_only/interest_only）。**V1 仅兴趣受众，无 custom/lookalike**。
- 衔接：投放模板 `audience_id` FK 引用（可 `audience_json` 内联覆盖）；被活跃模板引用的受众**拒绝删除**。

### 2.6 素材→模板的暗线
素材的 `country/language` 是贯穿轴：决定 AI 分析输出语言 → 表单 locale → 消息语言。素材 AI 文案有三条下游消费者：创意 headline/body（**随机挑一组，优先于模板手填**）、无消息模板时 Messenger 欢迎语兜底、无表单模板时 AI 自动建表单的输入。

**串联要点**：`assets.ai_copy_json` 是创意文案/欢迎语/AI 自动表单三条下游的唯一共同输入——它断（分析失败或结果为空）时**整条链不报错、只静默降级**，运营侧最该盯的是素材列表的 ai_status 角标而不是部署结果；落地页则记住"像素优先级靠 ads_cache 反查，缓存不新鲜会落到错像素"。

---

## 三、模板与部署：从配置到 FB 在投（LaunchTemplates 页）

### 3.1 三层结构模板（树模板，2026-09-08 上线，迁移 0088）
- **操作**：新建 FB 模板默认进**结构模式**（TT 仍平铺）——左树（系列→组→广告节点）+ 右表单；组可设预算/受众/优化目标覆盖，广告节点绑素材/文案/落地页/子码/消息/表单模板；**素材组节点**（一个广告节点挂多个素材）部署时每素材展开一条广告。
- **底层**：`launch_templates.structure` 单 JSON 列（组≤10/每组节点≤20/每节点素材≤50/每账户展开≤200 广告硬顶；lifetime 组必须带排期；跟帖节点强制单素材）。**节点开关默认 PAUSED**（用户拍板的安全默认）：campaign 恒 ACTIVE、组开关→adset 状态、**组开且广告开才 ad ACTIVE**，关闭的照建待开——默认部署出来是一棵全暂停的树，激活完全显式。保存时把"第一组第一广告"回写平铺列（旧读方不炸，新旧模式并存：structure 空=平铺母版模式）。
- **组级预算换算**：节点 USD → 目标账户本币 minor units，汇率取 `currency_rates` 当日值；**ABO 启用组预算求和 >$5000 直接 400 拦截**（资金安全闸）。

### 3.2 转化绑定
(objective, conversion_goal) 经 `_OPT_GOAL_MAP` 映射 optimization_goal；SALES+OFFSITE_CONVERSIONS **必须有像素否则预检 400**；LEADS 分流 Instant Forms（绑主页）或网站线索（绑像素）。树模式下每个广告节点按 objective 显隐消息/表单绑定项；**像素三级优先：组节点 pixel_id > 部署抽屉每账户手选 > 模板 pixel_id**。选落地页自动回填 landing_url=公开链接，子码下拉按页过滤。

### 3.3 预检（不花钱）
`POST /launch-templates/{tid}/preflight`：只构建 payload 不调 FB。预算守卫、**汇率缺失 400（不静默 1:1 兜底）**、占位符校验、payload 三段样例；树模式额外给 `will_spend` 将消耗清单（素材组按素材名列名）+ 逐组本币换算 + ABO 合计。注意：**子码存在性警告目前只在平铺模式**——树模式填了拼错/已归档的 slug 不会预检拦（已知缺口）。

### 3.4 部署抽屉
- **随机主页**：对已选账户从各自主页池随机分配、全批去重（同一主页不给两账户）、池小账户贪心先分、可重摇；跟帖(reuse)模式下禁用随机、按"哪个账户令牌能管该帖主页"预过滤。
- **像素策略**三档：跟随模板 / 随机用账户已有像素 / 每账户新建（失败自动降级随机）。
- **树概览卡**：N 组 × M 展开广告数、启用链路数、ABO 合计；确认弹窗分"有启用链路=立即消耗警示 / 全暂停"两套文案。

### 3.5 部署 runner（FB 侧建什么）
- 防重：advisory lock 115 把"查 running→建 job→commit"串成原子段，重复提交 409 → `launch_jobs/launch_job_items` 异步 BackgroundTasks → 逐 item 心跳 touch（防 10min reap 误判）→ **partial 是一等状态**（成功 X/Y）→ retry（批量=整 item 重跑；**树部分成功拒整树重跑**防重复广告，只能人工清理）。
- FB 创建顺序：**campaign → 逐组 adset → 逐广告 creative+ad**（素材组逐素材展开，广告名=素材名）。creative 二选一：`object_story_spec` 内嵌（image_hash/video_id）或 `page_post_id` → `object_story_id` 引帖（跟帖复用；App Live 后直接发帖被 pages_manage_posts 挡，发帖路径基本只留引用）。素材平台资产按账户缓存上传（image_hash/video_id 行锁合并）。
- TT 差异：素材先传 TT 文件库拿 `file_id`；campaign → **adgroup**（定向/预算/转化像素 code 全在这层）→ ad（**出生即 DISABLE**）；广告宏用 `__AID__`；无主页帖/表单链路；**树结构 400 拦**（本期不支持）。
- 部署后**对账**：逐成功账户刷新 ads_cache（清单/管理器立即可见）；TT 不对账，靠 15min cron 兜底。
- 追踪参数插值：landing_url 仅 **7 个白名单占位符**（campaign.name/adset.name/account.name/account.id/asset.name/template.name/platform）；**`{{ad.id}}` 显式拒绝**——历史事故：绑了占位符的子码像素不 fire（FB 建广告前拿不到广告 ID）。create/update/树节点/preflight/deploy/retry 六道卡点全覆盖。

### 3.6 子码归因链（部署与数据回流的桥）
```
模板绑子码 slug ─▶ 部署: effective_url = {落地页}/a/{slug}?ad={ad.id}   (TT: ?ad=__AID__)
   ─▶ FB 投放时替换 {ad.id} 宏 ─▶ 用户点击
   ─▶ Worker /a/{slug}: 防护判定(国家/UA/机房/频控/去重) 
        redirect模式: 直接 302 目标
        display模式: 调后端 route_next ─▶ 定像素(广告组>LandingPixel>页级)+定目标(广告覆盖>子码>页轮换)
                     ─▶ _d base64 ─▶ 302 落地页 ─▶ 页面 JS 解码 fire 像素(PageView+转化)
   ─▶ /events/ingest 回传: landing_events 落库(fired_pixel_ids=地面真相 + 来源归因:受控/外部/爬虫/机房)
        + 子码首点自动绑 ad_id(reserved→active)
   ─▶ 回流规则评估: conversion_source fb/landing/either ── landing_clicks(按 ip_hash 去重)/leads 计入
```
- 兜底 cron：`run_subcode_autobind`（1h，从创意 URL 反查 `/a/slug` 绑漏绑的 ad_id）；`run_subcode_cleanup`（每日，reserved 未绑 14 天→archived→30 天→deleted）。
- 树部署与子码：逐节点解析 slug、建后广告名标注 `[子码:slug]` 并回绑。

**串联要点**：子码归因链是全系统最长的一条链（模板→部署宏→FB 替换→worker→像素→ingest→回流），**任何一环断 = 广告部署成功但数据黑盒**——最脆的两处是 `?ad={ad.id}` 宏未被 FB 替换时会被当字面量绑死、以及树模式子码存在性没有预检拦截；另记住组级预算依赖当日汇率，非 USD 账户缺汇率行会整组失败。

---

## 四、运行期：数据回流、巡检止损、看板与管理

### 4.1 巡检引擎（`guard_engine.py`，系统心脏）
- `run_inspection` 每 5min（锁 101；频率可在设置调）：多租户 → 账户并行（默认 4 线程）→ 每账户一次 insights 调用（**time_range=近 7 天到账户本地今日**，分天行复用回填）→ **评估集 = 今日 insights ∩ live ACTIVE 广告**（live 拉失败落 ads_cache 兜底并计降级 streak，连续 3 轮告警 live_fetch_degraded）。
- **跳过原因**（都会留痕/告警）：无令牌、insights 失败（token_expired/permissions/rate_limited 等，6h 聚合告警）、**学习期（默认 0.0 小时=不保护**，曾默认 24h 致新广告空耗 $51 不被停后改）、当日加白（guard_ad_allowances，账户本地日）、哨兵 armed（只跳评估、快照照写）、KPI 解析异常（宁纵勿枉）、`[Tova-保活]` 系列豁免、被过滤的有消耗广告 → **coverage_lost 告警**。
- 无规则租户自动注入兜底规则 **bleed_abs $20**（花超 $20 且 0 转化即停）——资金安全网。

### 4.2 守护规则（10 类）与动作
| 类 | 触发（默认参数） | | 类 | 触发（默认参数） |
|---|---|---|---|---|
| bleed_abs 空耗止损 | 花超 $20 且 0 转化 | | trend_drop 效果下滑 | 今日 ROAS 较昨日跌 ≥40% |
| cpa_exceed 成本超标 | CPA > 目标×1.3 | | consecutive_bad 连续差 | 连 2 天快照 CPA>目标×1.3 |
| click_no_conv | 点击≥50 且 0 转化 | | budget_burn_fast 瞬烧 | 两轮巡检 spend 增量 ≥$20 |
| low_ctr_no_conv | 花≥$10+曝光≥100+CTR≤0.5%+0 转化 | | slow_scale 智能扩量 | 转化≥3 且 CPA≤目标×0.8 → +20%（cap $100/日，24h 冷却） |
| reach_no_conv | 触达≥1000+花≥$10+0 转化 | | roas_scale | ROAS≥3 → +15% |

- **转化口径** conversion_source：fb / landing / either（默认）；CPA 类与扩量类**强制 FB 口径** = max(conversions, leads 数)；landing 口径 = 通过量/访问量（ip_hash 去重）。KPI 目标（kpi_configs 的 target_cpa）优先于规则参数。
- **暂停执行链**（资金安全核心）：动作升级链 **广告→组→系列**，每级 pause 后 sleep 2.5s **回读 effective_status 核验**（假停检测，仍 ACTIVE 则升级下一级）；失败 5min 重试、成功 60min 冷却；权限永久错误写 24h 退避标记+critical；成功回写 ads_cache 根除假覆盖告警。全部动作落 action_logs（source=rule_engine）+ 通知（TG 带"加白今日"inline 按钮）。

### 4.3 多级防线与告警
| 防线 | 频率 | 职责 |
|---|---|---|
| 主巡检 | 5min | 规则止损/扩量 + 快照写入 |
| 哨兵 run_sentinel_patrol | 3min | **armed 账户 ACTIVE 系列全停**（kill-switch）；dedup 但仍回读重停；权限退避 |
| budget_alerts | 15min | 日预算进度 50/75/90/98 分档（98=critical，其余 warning；ads_cache 0 API） |
| watchdog | 10min | 巡检心跳停滞 15min / 单账户 30min 无巡检 / 令牌 debug_token 过期预警 |
| 紧急暂停 | 手动 | 一键全停：先同步 cache → 归并系列 → 逐系列 PAUSED → 回读核验 → **全量终验**（地面真相再扫一遍） |
| 保活 keepalive | 每日（**默认关**） | $5 主页赞防休眠，[Tova-保活] 前缀三处豁免 |

- **通知体系**（`notify_utils.py`）：`emit_notification` = 站内信必落 + TG 按角色路由（critical 发全租户已绑用户，warning/info 受用户级 prefs 门控，**critical 恒推、prefs 解析失败=全推 fail-open**）+ `dedup_recent`（查 action_logs 近 N 分钟同名事件）+ **风暴上限每租户每类型每日 30 条**（emergency_pause/coverage_lost/sentinel_pause 等 critical 豁免不封顶）；TG 连续 3 败发 tg_channel_down critical。

### 4.4 数据看板（Dashboard，北京业务日双基准）
- **双基准不可混**：`perf_snapshots.snapshot_date` 按**账户本地日**存；查询按**北京日历日字符串**比较——纯字符串区间天然命中各账户的本地当日。加白是另一套基准（账户本地当日，对齐巡检）。
- KPI 卡（消耗/转化/CPA/ROAS/潜客/CPL+昨日对比）；**余额 = spend_cap − amount_spent**（明确不用 FB balance 字段）。perf_snapshots（22 列）**唯一写入者 = 巡检**；tick 表存 5min 账户级聚合做细粒度趋势（拉取失败 fill-forward 复制上轮）。
- 落地页数据 Tab：访问/通过/屏蔽统计 + 明细，来自 landing_events。

### 4.5 广告管理器（AdManager）
- **读**：三层（系列→组→广告）全部走 `ads_cache`（15min cron 全量 + 部署后对账 + 巡检回写广告层），0 FB 调用；perf 由 perf_snapshots 聚合；拒审广告弹 review_feedback 详情。
- **写**：暂停/恢复/改日预算（×1.2/×1.5/×2 快捷倍率）/改名/删除/跳转链接管理（单广告重定向 overrides）——全部 is_managed 门 + **回读验证 + cache patch**。
- **诊断**：实时 insights + KPI resolver + 落地 click/visit + 逐条规则复算 + 冷却/加白/子码状态。
- **潜客 CRM**：leads 表双通道（10min 轮询兜底 + webhook 实时，lead_id 全局去重），状态机 new/contacted/won/lost。
- **主页受控面板**：遍历令牌 `me/accounts` 的 permitted_tasks 判 can_manage/can_advertise，多令牌同页权限 OR 合并（≠ certified_pages——后者是 TW/SG/HK 受监管地区的认证主页+身份库，供 adset regional_regulation_identities 用）。

**串联要点**：运行期一切（快照/趋势/规则/预算告警/潜客选广告）都架在两块地基上——**令牌可用**（全链单点，全灭只靠 6h 聚合+watchdog 30min 兜底）和**巡检活着**（perf_snapshots 唯一写入者是巡检，巡检停 15min 才告警，tick 的 fill-forward 会把旧值伪装成新数据）；部署后前 15 分钟看板/管理器数据不全是正常的（等对账+cron）。

---

## 五、支撑体系（贯穿全链的地基）

| 支撑 | 机制 | 关键事实 |
|---|---|---|
| 多租户 RLS | `get_db`=RLS 受限 / `SuperSessionLocal`=BYPASSRLS | PG 行级策略按 `app.tenant_id` 会话变量；**cron/部署 runner/注册全走 BYPASS，代码里必须显式 filter tenant_id**（三次踩坑史：忘 filter=看到别家数据；RLS 上下文忘设=静默 0 行） |
| 调度锁 | advisory lock 101-118 | gunicorn 4 worker 各起 scheduler，`pg_try_advisory_lock` 防重复跑（重复=重复停广告/TG spam）；**118 已被 KPI AI 占用，新增从 119 起** |
| RBAC | 16 权限 key + owner/operator/finance 兜底 + 自定义角色(JSONB) | JWT 解码→membership 复查（被移除/降权立即踢 token）；多团队 switch-tenant 重 mint |
| i18n | zh/en 全站 | 通知语言=租户 owner 的 locale；HTTP 报错按 `X-Locale` 查表译英；改文案两份 locale 同步、en 零 CJK |
| TG 通知 | 租户级+用户级双绑定 | 绑定走 t.me 短码（30min TTL）→ webhook /start → **SuperSessionLocal 写**（RLS 静默 0 行历史真因）；一人可绑多 chat |
| 审计日志 | action_logs + trace_id | `X-Trace-Id` 中间件贯穿请求；source 覆盖 user/rule_engine/sentinel/launch/sync 等 20+；日志中心三视图（操作/系统/用户活动）可按 trace 拉全链路；登录限速 15min 5 次 |
| 汇率 | `fx_sync` 每日 3:07 | 源=open.er-api.com（免费无 key）；**失败保留旧值且无告警**；消费三处：止损阈值换算 / 看板本币 / 模板 USD→本币预算 |
| 备份 | 超管手动 `POST /admin/backup` | pg_dump→gzip→`/opt/toveads/backups`，30 天滚动；**无下载、无异地、无自动 cron**——服务器盘损=代码+备份同灭 |
| KPI 解析 | resolve_kpi 级联 | L0 手动 kpi_configs → L4 系统矩阵 → L1/L2 AI 纠偏（xact lock 118+配额退避）→ L5 语义兜底；解析异常跳过评估"宁纵勿枉" |
| 系统设置 | system_settings KV | 调度频率/巡检并发/学习期/风暴上限/保留天数/低额阈值等全热读（部分改后免重启 live reschedule） |

**串联要点**：支撑体系里最容易被忽视又最致命的三个——**BYPASSRLS 双向坑**（super session 忘 filter、普通 session 忘设上下文，都是静默错而非报错）、**备份是单点**（只在本机、纯手动）、**守护体系自身没有 watchdog**（汇率断供无告警、新 critical 事件忘加风暴豁免会被 cap 吞掉）。

---

## 附：16 个定时任务速查（APScheduler + advisory lock）

| 任务 | 频率 | 锁 | 一句话 |
|---|---|---|---|
| run_inspection | 5min | 101 | 主巡检：insights→规则→止损/扩量+快照写入 |
| run_sentinel_patrol | 3min | 106 | 哨兵 kill-switch：armed 账户 ACTIVE 系列全停 |
| run_budget_alerts | 15min | 102 | 日预算进度 50/75/90/98 分档告警 |
| run_watchdog | 10min | 103 | 巡检心跳/单账户停滞/令牌健康 |
| run_reassociate | 2h | 104 | 孤儿账户重绑令牌+still_orphan 告警 |
| run_subcode_autobind | 1h | 105 | 从创意 URL 反查绑子码 |
| run_landing_block_scan | 60min | 107 | 落地页 FB 屏蔽探测+告警 |
| run_subcode_cleanup | 每日 4:17 | 108 | 子码归档/删除 |
| run_keepalive | 每日 2:17 | 109 | 防休眠（默认关） |
| run_account_status_sync | 30min | 110 | 账户状态/余额/disable_reason+像素同步 |
| run_ads_cache_sync | 15min | 111 | 广告结构三层缓存全量刷新 |
| _reap_launch_stale | 5min | 116 | 部署孤儿 job 回收（心跳>10min） |
| run_leads_poll | 10min | 117 | 潜客轮询兜底（2h 窗口） |
| run_stale_page_cleanup | 每日 5:17 | 117⚠ | 无主主页清理 |
| run_data_retention / run_fx_sync | 每日 4:33 / 3:07 | - | 保留清理 / 汇率刷新 |
| run_tt_token_refresh | 6h | 114 | TT 令牌 24h 过期前轮换（刷新即轮换） |

⚠ 实测锁号冲突（本文核实发现，代码现状）：`leads_poll.py:27` 与 `routers/leads.py:487` **共用 117**——每日一次的无主主页清理若恰逢轮询持锁会整个跳过当天（概率低但存在）；另 118 已被 KPI AI 互斥占用，新增锁号应从 119 起。

## 附：当前已知边界（如实，2026-09-08）
- 三层树模板已上线（29 断言 smoke 全绿），**真花钱部署待用户授权实测**；TT 不支持树结构。
- FB App 审核中：read_insights/pages_manage_posts 未获批 → Page 级数据/直接发帖/webhook 订阅受限；FB BM policy 限制部分恢复中。
- TT 全链路（含表单部署 UI）等 sandbox 实测；WhatsApp 消息模板/感谢页按钮=本地存储+预览，FB payload 不带（显式半接线）。
- 保活默认关闭；备份无异地。

---
*核实基准：backend/app 代码 @ 2026-09-08（git 12664d5）+ TECH_REVIEW 批次记录；锁号/默认参数/表列均逐一读码确认。数字与 CLAUDE.md/ARCHITECTURE.md 冲突处以本文（更新）为准。*

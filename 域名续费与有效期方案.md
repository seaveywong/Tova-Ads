# 域名生命周期与续费方案 — 2026-09-27 稿（批2 核心，待拍板）

> 调研依据：Dynadot REST API 实测/文档实证（domains/list、domains/{d}、POST renew、PUT renew_option
> 均存在且 signed 可用；register 响应含 expiration_date；到期 webhook DOMAIN_EXPIRED 有
> 30/10/3/今天/赎回五档分组；价目表含 renewal 价与宽限期信息）。
> 现状缺口：LandingDomain 无 expires_at/auto_renew/registrar 字段，UI 无任何有效期展示，
> 无续费入口/提醒/自动续费。

## 一、数据层（迁移 0108）
LandingDomain 增列：
- `expires_at` DateTime — 到期日（注册响应写入 + 每日同步对齐）
- `auto_renew` Boolean default true — 平台侧自动续费开关（行内可切换）
- `registrar` Text — dynadot / porkbun / external（自有域名无注册商，不参与续费）
- `last_renewed_at` DateTime — 最近续费时间（详情展示）

## 二、我的域名 Tab 展示（核心 UX）
行级新增（对齐"状态一目了然"原则，到期近的排前面）：
```
[域名] [来源] [DNS] [有效期至 2027-09-26 · 剩 365 天] [自动续费✓] [子域数]
```
到期状态四档 chip：
| 状态 | 展示 | 颜色 |
|---|---|---|
| >30 天 | 有效期至 date | 灰（正常） |
| ≤30 天 | 「30天内到期」+日期 | 黄 |
| ≤7 天 | 「7天内到期」 | 红 |
| 已过期（宽限期内） | 「已过期·可原价续费」 | 深红 |
| 赎回期 | 「已过期·需赎回（费用高）」 | 深红+联系超管 |

- Tab 角标 = ≤30 天域名数（有问题在上原则）
- 展开详情区补：注册商 / 注册时间 / 到期时间 / 自动续费开关（行内切换）/ 最近续费

## 三、手动续费（域名行「续费」按钮，≤90 天可续）
弹窗：当前到期日 → 选 1-10 年 → 续费价（价目表 renewal × 年 + 手续费，**单一总价口径同购买**）
→ **余额扣款**（余额不足 → 提示先充值，不做续费的 USDT 直付单——续费低频，统一走钱包简化对账）
→ API renew → 更新 expires_at → 通知「已续费至 X」。

## 四、自动续费（每日 cron，锁号 120）
- 默认**开**（逐域名可关；与"无保护期"铁律不冲突——这是资产保护不是广告学习期；余额不足**绝不垫付**）
- T-14：余额够 → 扣款+续费+通知；不够 → 通知「余额不足，14 天后到期，请充值」
- T-3 再试一次 + 再提醒
- 注册商侧失败 → 告警超管人工（failed 留痕可重试）
- 注册商侧同步开 renew_option（双保险：平台侧为主，注册商侧兜底只对平台账户余额有意义——
  实际我们的钱都在平台钱包，注册商侧 renew_option **不开**，避免注册商余额被动扣——平台统一管控）

## 五、到期提醒（通知中心 + TG，billing 域事件带租户）
30/14/7/3/1 天五档 → owner + finance；每档一次（dedup）。本地 cron 扫描驱动（不依赖 Dynadot webhook，
后者后续可接作兜底）。

## 六、过期状态机与落地页联动
```
正常 → 到期 → 宽限期(约0-20天,原价可续) → 赎回期(restore ~$100,人工) → 释放
```
- 宽限期：续费按钮可用（原价）；赎回期：显示费用引导找超管
- 已过期域名在落地页绑定下拉中禁用（防新建页绑到快死的域）

## 七、每日同步（同 cron）
拉注册商域名列表 → 对齐本地 expires_at（用户可能直连注册商续费过）+ 发现本地未登记域名
（与 CF 对账面板互补）。

## 工作量
迁移+模型 ~60 / cron（同步+自动续费+提醒）~240 / 续费端点 ~120 / 前端（行展示+开关+续费弹窗+详情+角标）~250 / i18n ~40 键。

## 待拍板决策点
1. 自动续费默认：开（推荐，资产保护）or 关（保守）
2. 续费支付：仅钱包余额（推荐，简化）or 也支持 USDT 直付单

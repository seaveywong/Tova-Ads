# 方案：投放模板选落地页 → 部署时自动创建投放链接（子码）

> 2026-09-08 调研稿（只调研不改代码）。目标需求：创建广告模板时就选好落地页，部署时**自动创建投放链接/子码**；未绑定落地页的广告节点 = 自定义 URL 直投。且全链真实跑通——落地页像素配置（FB/TT/S2S token/event_id）在自动创建的链接上正确 fire。
>
> 所有行号基于当日工作区代码，文件路径均相对 `toveads/`。

---

## 0. 结论速览

| 问题 | 现状 |
|---|---|
| 模板选落地页后，部署时子码能自动建吗？ | **不能。子码必须预先在落地页模块手动生成，再在模板编辑器下拉手选**（`/subcodes/generate` → 编辑器下拉）。部署链只会"查这个 slug 存不存在"，不存在就静默丢追踪。 |
| `/a/{slug}?ad={ad.id}` 宏链路闭环吗？ | **链路设计闭环，但部署产出的宏写法有 P0 疑似 bug**：`ad_ops.py:226` 与树 runner `launch_templates.py:2220` 用**单花括号 `{ad.id}`**，而 Meta 文档、1.0 生产验证、2.0 自己的 `/launch/create`（`launch.py:182`）和 Landing.vue 复制链接（`Landing.vue:359/372`）全部用**双花括号 `{{ad.id}}`**。单花括号不在 Meta 文档宏列表里，未被任何真实 FB 部署验证过——若 FB 不替换，整条归因链死。 |
| 像素 fire 用哪份配置？ | display 模式下 worker 调 `route_next` 动态解析：**FB 像素按点击的广告（ads_cache → adset promoted_object.pixel_id）→ LandingPixel 按账户 → 页级 pixel_ids 三级回退**（FB 动态像素已实现）；**TT 像素只有页级**（无按账户动态解析）。浏览器 fire 的像素来自 route_next 返回值（经 `_d` 注入页面），页级 baked 配置只是无 `_d` 时的兜底。 |
| S2S event_id 链？ | route_next 生成 UUID → S2S（TT Events API / FB CAPI 灰度）与浏览器 `fbq('trackSingle',…,{eventID})` 用**同一个 UUID**（FB 去重闭环）；**TT 浏览器端 `ttq.track` 未带 event_id**（去重未闭环，既有缺口）。 |
| 推荐自动建链粒度 | **每广告（展开后每条真实 FB 广告一个子码）**，详见 §4.2。 |
| 最大风险 | ①宏写法 bug（不修则全链归因死）；②链接行数爆炸（200 广告/账户/次 × N 账户）污染子码表与 FB 封禁扫描。 |

---

## 1. 现状链路盘点（读码结论）

### 1.1 模板编辑层（frontend `src/views/LaunchTemplates.vue`）

- 平铺模式与树模式广告节点都有三件套：`landing_page_id`（下拉选已发布落地页）/ `landing_url`（手填 URL）/ `subcode_slug`（子码下拉）。
  - 平铺 UI：2485-2496；树广告节点 UI：2631-2641。
- 选落地页的联动 `onLandingChange`（337-347）/ `onNodeLandingChange`（1068-1080）：
  - `landing_url = p.public_url`（**自动回填，但 `public_url = custom_domain or ""`**，见下）；
  - 子码下拉过滤为本页子码（`subcodesForLanding` 545-548 / `subcodesForNode` 1068-1071，数据来自 `GET /subcodes?page_id=…&status=all`）；
  - 换页时清掉不属于新页的旧 slug。
- **子码只能从"已存在的子码"里选**——用户必须先去落地页模块（`Landing.vue` 341 行 `POST /subcodes/generate`）逐个手动生成。编辑器里没有"自动生成"入口。
- 保存时后端 `_validate_structure`（`launch_templates.py:92-201`）只校验形状/素材/预算，**不校验 subcode_slug 是否存在**；平铺预检（786-794）才有存在性检查（status ∈ reserved/active，缺失 → `subcode_warn_slug` 警告，前端 2902 行渲染）。
- `landing_page_id` 的全部实际用途（迁移 0054 注释明说"部署仍用 landing_url + subcode_slug"）：
  1. 编辑器过滤子码下拉（前端）；
  2. **TT 模板的像素解析** `_resolve_tt_pixel`（`launch_templates.py:1251-1259`，取 `lp.tt_pixel_ids[0]`）——这是它唯一参与部署的地方，且只对 TT 生效。
  - **FB 部署完全不读 `landing_page_id`**：FB 链路 effective_url 的 base 是"节点/模板的 `landing_url` 字段"（选页时复制下来的快照），不是部署时从落地页行解析。

### 1.2 部署层（backend `app/routers/launch_templates.py` + `core/ad_ops.py`）

三条 FB 部署路径的子码处理：

| 路径 | 子码来源 | effective_url | 回绑 |
|---|---|---|---|
| 平铺单模板（`_deploy_series_fb` 1848-1891 → `deploy_one_account`） | `tpl.subcode_slug`，runner 2348-2353 查 `LandingAdLink` | `ad_ops.py:223-226`：`{landing_url}/a/{slug}?ad=` + **`{ad.id}`（单花括号）**；`landing_url` 空时 base 兜底 `https://tovaads.com`（**该域无 /a/ worker，死链**） | `ad_ops.py:279-285`：广告名加 `[子码:{slug}]` + `link.ad_id = ad_id`、status=active |
| 树模式（`_deploy_item_fb_tree` 1937-2301） | 每广告节点 `anode.subcode_slug`，2114-2121 查询缓存（**不过滤 status，archived 的 slug 也会被用**） | 2216-2220：同上构造，**同为单花括号 `{ad.id}`** | 2269-2276：同上。注意素材组节点展开的多条广告共用同一 node_slug → `link.ad_id` 被最后一条覆盖（last-wins），真正的逐广告归因靠 `?ad=` 宏 |
| 手动单发（`routers/launch.py` `/launch/create` 173-184） | `body.subcode_slug`，查不到直接 400 | `launch.py:182`：`?ad=` + **`{{ad.id}}`（双花括号，正确）** | 232-234 同上 |

- slug 查不到时（平铺/树）：`link = None` → `effective_url = _lp_url`（裸落地 URL）→ **静默丢追踪，部署照常"成功"**。平铺有预检警告；**树模式预检 `_preflight_tree_fb`（913-1061）完全没有子码存在性检查**——2113 行注释"预检已拦"与实现不符，是注释性错误+实际缺口。
- 追踪参数插值 `_interp_landing_url`（694-713）只解静态白名单占位符；`{{ad.id}}` 在保存/部署/重试三道门都被明确拒绝（669-691，历史事故 `subcode-placeholder-binding-bug` 的守卫）——**这是对的**：占位符不允许进子码的 ad_id 字段，但允许作为 URL 宏由 FB 在投放侧替换。
- TT 部署（`ad_ops.py:333-382`）：`?ad=__AID__`（TT 宏），worker 同样从 `?ad=` 读（`landing.py:146`）。
- 部署收尾 `_refresh_ads_cache_after_deploy`（1713-1734）：把新广告拉进 ads_cache——**这是 route_next 按广告动态解析像素的数据前提**。
- 死代码：`LaunchJobItem.subcode_slug` 列（`models/launch_template.py:115`）只读不写（`_item_dict` 2474），从未被赋值。

### 1.3 点击层（worker + route_next）

发布落地页时（`landing.py:_do_publish` 285-375）：页级像素配置（`pixel_ids`/`tt_pixel_ids`/转化事件）baked 进页面 HTML（`__LP_PIXELS_JSON__` 等）+ `LP_CONFIG`（route_next_url、ingest、防护、preview token）prepend 进 worker。

用户点击 `https://页域名/a/{slug}?ad={fb替换后的广告id}`（`landing.py` WORKER_SOURCE 82-200）：

1. **146-148**：解析 slug、`adId`（`?ad=`/`?ad_id=`）、`actId`（`?act=`/`?act_id=`——**FB 部署链不拼 act 参数**，act 靠 route_next 从 ads_cache 反查）；
2. 防护链（爬虫内置拦截 92-95、国家/设备/UA/频次/防重）；
3. **redirect 模式（176-181）：直接 302 到页级 target，不 fire 任何像素**；display 模式（默认）走下；
4. **182-198**：POST `route_next`（secret+slug+ad_id+act_id）→ 返回 `{target_url, pixel_ids, tt_pixel_ids, tt_conversion_events, tt_event_id, conversion_events}` → base64 编码进 `_d` → 302 到 `/?_d=…`；同时把 visit 事件（含 `pixel_ids` = 本次真实将 fire 的像素，地面真相）发 ingest；
5. 落地页 HTML（44-79）：`_d` 解码 → `LP_PIXELS`（**`_d.p` 优先，页级 baked 兜底**，56-57 行）→ 逐像素 `fbq('init')+trackSingle('PageView')`；CTA 点击 `trackConversion`（74 行）：FB 用 `{eventID:_eid}`，**TT `ttq.track` 不带 event_id**。

`route_next`（`routers/landing_events.py:296-469`）：

- **跳转目标三级**（317-351）：`AdRedirectOverride`（按 ad_id 的广告级覆盖）> 子码 `target_urls` > 页级轮换（sequential/random/first）；
- **FB 像素三级**（353-406）：
  1. ads_cache 找到该 ad_id → 其 adset → `promoted_object.pixel_id`（**按广告动态像素，已实现**）；
  2. `LandingPixel` 按 act_id（候选序：ads_cache 反查的 act > `?act=` 透传 > `link.act_id`）；
  3. 页级 `pixel_ids`。
- **TT 像素：只有页级**（417-429，注释明说"现阶段=页级 fallback，无 TK adset 动态解析"）——**多账户共用同一落地页时，TT 像素无法按点击所属账户自动选，全部 fire 页级同一批 TT 像素**；
- FB CAPI 灰度像素（430-438，`landing_pixels.fb_capi_enabled` 默认关）；
- **event_id**（439-460）：有 TT 像素或 CAPI 像素时生成 UUID，S2S 在 route_next 内直接 fire（`tk_events.py` 的 `send_tt_s2s_for_visit`/`send_fb_capi_for_visit`，同一 UUID），`dry_run`（发布自检/健康检查）不发假转化。

### 1.4 归因回绑层（双路径 + 清理）

- **首点自动绑**：`ingest_event`（`landing_events.py:163-172`）——slug+ad_id 齐且 ad_id 不含 `{{` → 绑 `LandingAdLink.ad_id`（校验 slug 归属本页，防跨页劫持）。
- **创意反查兜底**：`guard_engine.run_subcode_autobind`（2842-2902，1h）——未绑子码存在时拉各账户创意链接，正则 `/a/([A-Za-z0-9_-]{4,64})` 反查绑 ad_id（FB 账户 only）。部署侧漏绑时这条能兜住（**自动建链方案与此天然兼容**）。
- **闲置清理**：`run_subcode_cleanup`（2905-2975，每天）——reserved + 未绑 + 0 访问 + 超 14 天 → archived；archived 超 30 天 → deleted（清配置可恢复，`/subcodes/{sid}/restore`）。

### 1.5 「自定义链接」分支现状

- 广告节点 `landing_page_id=0`（前端选「手动填 URL」）且 `landing_url` 非空 → effective_url = 插值后的裸 URL 直投 FB。**不经 /a/**：无 visit 日志、无子码归因、无 route_next 动态像素——像素只在目标页自己的像素体系里 fire（与本系统无关）。这是设计如此，方案需保留该分支。

---

## 2. 断点清单（按严重度）

| # | 级别 | 断点 | 位置 | 说明 |
|---|---|---|---|---|
| B1 | **P0** | **宏写法单花括号 `{ad.id}`** | `ad_ops.py:226`、`launch_templates.py:2220` | Meta 文档宏是 `{{ad.id}}`（[dynamic URL parameters 规范](https://www.facebook.com/business/help/2360940870872492)）；1.0 生产（index.html 全量）、2.0 `launch.py:182`、`Landing.vue:359/372` 都是双花括号。单花括号未被验证——FB 若不替换，worker 收到字面量 `{ad.id}`：归因死、`ingest_event` 的 `{{` 守卫（landing_events.py:167）抓不住单花括号，还会把垃圾值绑进 `link.ad_id`。**2.0 子码链接从未经过真实 FB 花钱部署验证**（树模板 memory：真部署待授权）。 |
| B2 | **P0** | **无自动建链** | 全部署链 | 子码必须预建手选；选了落地页但没选子码 → 裸 URL 直投页根（像素 fire 页级配置但**零归因、零 visit 日志、零广告级像素**）。 |
| B3 | **P1** | 树模式预检不查子码存在性 | `_preflight_tree_fb` 913-1061 | 平铺有 `subcode_warn_slug`，树没有；2113 注释"预检已拦"失实。拼错/已删 slug = 静默丢追踪。 |
| B4 | **P1** | 树 runner 查 slug 不过滤 status | `launch_templates.py:2118-2120` | archived/deleted 的 slug 照样被解析使用（平铺预检口径是 reserved/active）。 |
| B5 | **P1** | `public_url = custom_domain or ""` | `landing.py:786` | 只有 bound_subdomains / pages.dev 的页 public_url 为空 → 编辑器回填 landing_url 落空 → 部署 base 兜底 `https://tovaads.com/a/…` 死链（`ad_ops.py:225`；前端复制链接用的是 `bound_subdomains[0]`，`Landing.vue:365-368`，两处口径不一致）。 |
| B6 | **P1** | **TT 像素无动态解析** | `landing_events.py:417-429` | 多账户（尤其 TT 多广告主）共用一页时 fire 同一批页级 TT 像素；FB 已有按广告/账户动态，TT 没有。 |
| B7 | **P2** | `link.ad_id` last-wins | `launch_templates.py:2269-2276`、`ad_ops.py:284` | 一 slug 多广告（树素材组展开/平铺跨账户共享）时 ad_id 列只留最后一条；逐广告归因靠 `?ad=`（日志层正确），但子码列表的 1:1 语义失真（列表另有 ad_count 聚合兜底）。 |
| B8 | **P2** | TT 浏览器 `ttq.track` 无 event_id | `landing.py:74` | TT S2S 与浏览器双发无法按 event_id 去重（tk_events.py 头注释要求的同 UUID 只在 FB 侧闭环）。 |
| B9 | **P2** | landing_url 是选页时的快照 | 编辑器 337-338/1073-1074 | 页后来换域名/加子域，模板 URL 不跟随（部署不回读落地页行）。 |
| B10 | P2 | `LaunchJobItem.subcode_slug` 死列 | `models/launch_template.py:115` | 只读不写；自动建链后每 item 的实际 slug 无处落账（可启用此列）。 |
| B11 | P2 | redirect 模式不 fire 像素 | `landing.py:176-181` | 页配置为 redirect 时自动建链的像素链整体无效（需部署前校验页 redirect_mode=display）。 |

---

## 3. 「已通 / 未通」总结

**已通（设计+代码在位）**：
- 落地页发布带像素配置 → worker + 页面 baked 兜底（landing.py `_do_publish`）；
- /a/{slug} 点击 → route_next 动态解析（FB 像素按广告→账户→页级三级）→ `_d` 注入 → 浏览器 fbq fire → visit 日志记真实 fired 像素；
- FB CAPI/TT S2S 与浏览器 FB 事件同 UUID 去重（CAPI 灰度默认关）；
- 归因回绑双路径（首点自动绑 + guard 1h 创意反查）+ 闲置清理；
- 占位符 `{{ad.id}}` 禁入子码字段的三道守卫。

**未通（本方案要解决/补齐）**：
- 部署时自动建子码（B2）——核心需求；
- 部署产出宏写法疑似错误（B1）——**先于一切修掉**；
- 选页→URL 的可靠解析（B5，自动建链必须部署时从落地页行解 base，不能信快照）；
- 树模式子码校验（B3/B4）；
- TT 按账户动态像素（B6）。

---

## 4. 方案：部署时自动创建投放链接

### 4.1 触发条件与「自定义链接」并存逻辑

```
广告节点解析 effective_url（部署 runner 内，逐广告）：
1. landing_page_id > 0（绑定了落地页）：
   base = 落地页行的公网 base（服务端解析：custom_domain → custom_domains[0]
          → bound_subdomains[0] → https://tovaads-landing-{id}.pages.dev，
          与 subcodes.py:_resolve_page_base 同口径，**不再读节点 landing_url 快照**）
   ── 子码策略（部署抽屉选项，不预填默认，见 4.3）──
   a. 自动（每广告一个）：无既有 slug → 建 LandingAdLink(reserved, page_id=落地页,
      act_id=本账户) → effective_url = f"{base}/a/{slug}?ad=" + "{{ad.id}}"
   b. 手动选既有 slug（现状行为保留，含跨账户共享一个 slug 的用法）
   c. 不用子码：effective_url = base（裸页直投，保留现状：像素走页级 baked，无归因）
2. landing_page_id = 0 且 landing_url 非空：自定义 URL 直投（现状零改动）
3. 两者皆空：现有校验/预检报错（AWAINESS 类目标除外，维持现状）
```

- 前提门（部署端点 400 快失败）：`landing_page_id` 对应页 `status=published`、`redirect_mode=display`（redirect 页建链无意义，B11）、有可达 base。
- 自动建的 slug 解析出来后落 `LaunchJobItem.subcode_slug`（激活 B10 死列）+ item metadata，部署清单可见"本账户建了哪些链接"。

### 4.2 粒度选择：每广告 / 每组 / 每账户

先明确一个事实：**归因与像素正确性与 slug 粒度无关**——逐广告归因来自 `?ad={{ad.id}}` 宏 + ads_cache 动态像素解析，slug 只是路由入口。粒度真正影响的是：封禁/屏蔽爆炸半径、`link.ad_id` 1:1 语义、子码表规模与清理压力、FB 审核面。

| 维度 | 每广告（展开后每条 FB 广告一个） | 每组（adset 一个，组内广告共用） | 每账户（一账户一个） |
|---|---|---|---|
| 逐广告归因 | ✅（?ad= 宏，与粒度无关） | ✅ 同左 | ✅ 同左 |
| FB 动态像素 | ✅（ads_cache 按 ad_id） | ✅ | ✅ |
| 封禁爆炸半径 | 最小：单链接被封/被屏只废一条广告 | 中：整组广告同时死链 | 最大：全账户广告同死 |
| `link.ad_id` 语义 | 真 1:1（回绑准确） | 组内 last-wins | 全账户 last-wins（B7 放大） |
| 子码行数 | `广告数×账户数`（上限 200/账户/次部署） | `组数×账户数`（≤10×N） | `账户数` |
| FB 审核面 | 大量同域不同 path URL | 中 | 少 |
| `run_subcode_autobind` 反查 | 每链接独立命中 | 一 slug 多 URL 命中多次 | 同左更集中 |
| 1.0 对标 | ✅ **1.0 生产即此模型**（index.html:14104"自动生成广告级子码 /a/子码?ad={{ad.id}} 回绑广告"） | — | 1.0 另有"账户绑定链接"作入口，但投放仍展开为广告级 |
| 清理压力 | 大（见 4.4，需死亡广告联动归档） | 小 | 最小 |

**推荐：每广告一个（方案 a 为默认选项）**。理由：①1.0 生产已验证此模型，用户认知与运维习惯连续；②封禁爆炸半径最小——FB 屏蔽扫描（每小时 fb_ban_scan/fb-check-batch）发现某 slug 被封时只影响一条广告，配合单链接重生成即可救活；③`link.ad_id` 真 1:1，子码表即"广告↔链接"台账，与既有列表/回收站/恢复 UI 完全兼容。行数压力是可管理的（4.4），且 _TREE_ADS_MAX=200 已经把单次部署上限卡死。
每组粒度收益模糊（归因既已靠 ?ad=，组级链接只剩"少几行"），不推荐做独立选项；每账户粒度可作为抽屉里的第三个选项保留给"一账户一链"的老习惯用户（与 1.0 账户绑定链接用法对应），实现成本与 a 相同只是建链时机提前到 item 开工。

### 4.3 slug 策略与部署抽屉选项

- **slug 生成**：沿用 `_gen_slug`（subcodes.py:18-26）——6 位 `a-z0-9` 随机、唯一性检查、碰撞重试 10 次。**不引入语义化前缀**（语义 slug 泄露结构、提高撞名/伪造成本，且 `[子码:{slug}]` 广告名标注已提供可读性）。
- **不预填默认**（用户铁律 `no-prefilled-input-defaults`）：部署抽屉增加"投放链接"单选（每广告自动 / 手动选既有 / 不用子码），**默认空=沿用模板内已选 slug 的现状行为**；用户显式选"每广告自动"才走自动建链。模板编辑器的子码下拉保留（手动路径）。
- 归属字段：自动建的 link 记 `page_id=节点落地页`、`act_id=本部署账户`、`status=reserved` → 建广告成功后回绑 `ad_id` + `status=active`（与现有 ad_ops 回绑同一模式，B7 在每广告粒度下自然消除）。

### 4.4 命名 / 清理规则（关联既有子码回收站）

- **失败自愈**：建链后建广告失败 → 链接停留 reserved+未绑+0 访问 → 既有 `run_subcode_cleanup` 14 天自动归档、30 天硬删，**零新增清理逻辑**。
- **死亡广告联动（新增，量小）**：`run_subcode_cleanup` 增补一条判据——active 且 `link.ad_id` 对应广告在 ads_cache 中 `effective_status ∈ {DELETED, ARCHIVED}` 连续超 N 天（建议 30，可配）→ 归档并入回收站（可恢复，不硬删）。防止"每广告一链"长期堆死行。
- **重复部署**：同模板同账户重跑 retry 时，reserved 且已绑本 job item 的 slug 直接复用不重建（防重试翻倍链接；树模式重试本就限全败 item，天然低风险）。
- 广告名标注 `[子码:{slug}]` 保留（FB 后台可追溯）。

### 4.5 S2S 动态像素补齐（TT 按账户）

现状 FB 已动态（ads_cache adset promoted_object → LandingPixel by act → 页级），TT 只有页级（B6）。补法（`landing_events.py:417-429` 处）：

```
tt_pixel_ids 优先级（对齐 FB 分支）：
1. LandingPixel(platform='tt', act_id=候选act, status='active')（候选序同 FB：
   ads_cache 反查 act > ?act= > link.act_id）
2. 无命中 → 页级 tt_pixel_ids（现状）
```

自动建链按账户记 `act_id` 后，`link.act_id` 候选天然准确——**每广告粒度下 TT 动态像素随本方案免费获得**。S2S token/event_id 链无需改动：route_next 生成 UUID → S2S（tk_events `send()` 带 `event_id`）→ 浏览器 fbq `{eventID:_eid}` 同 UUID；TT 浏览器侧 event_id 未接（B8）单列为后续项，不阻塞本方案。

### 4.6 像素正确性保障清单（逐环节断言点——实施+smoke 必须逐条验证）

> 铁律 `bare-except-silent-failure`：每条断言必须验**数据落点**，不是"没报错"。

| # | 环节 | 断言 |
|---|---|---|
| A1 | 部署·URL 构造 | effective_url 以 `/a/{slug}?ad={{ad.id}}` 结尾且为**双花括号**；base 来自落地页行解析非快照 |
| A2 | 部署·建链回绑 | FB 建广告成功 → DB 中 link.ad_id=返回 ad_id 且 status=active；失败 → link 仍 reserved（等清理） |
| A3 | 部署·ads_cache 对账 | job 完成后 ads_cache 能查到新 ad_id 且带 adset_id（route_next 动态像素的数据前提，`_refresh_ads_cache_after_deploy`） |
| A4 | FB 宏替换 | 真 FB 部署后创意 URL（Graph API 读回）中 `{{ad.id}}` 被替换为数字 ad id；worker 日志/visit 事件 `ad_id` 为纯数字（**B1 的终极验证，只此一法**） |
| A5 | worker 注入 | 点击 /a/{slug} → 302 到 `/?_d=…`；解码 `_d` 含 p（FB 像素）/tp（TT 像素）/eid（UUID）/t（target） |
| A6 | fire 的 pixel 来自 route_next | `landing_events` 表 visit 行 `fired_pixel_ids` 非空且 = route_next 解析结果（多账户复用时按点击所属账户，非页级固定值） |
| A7 | S2S event_id 一致 | route_next 日志（tk_events "event_id=…"）与浏览器 fbq 请求的 eventID 同 UUID；FB CAPI 灰度像素时双发同 ID |
| A8 | 归因回绑 | 首次点击后 link.ad_id 被自动绑（ingest 路径）；删绑后 1h 内 guard autobind 兜底（反查路径） |
| A9 | TT 动态像素（补齐后） | 同页两账户广告分别点击，visit 行 fired tt_pixel_ids 各自对应账户的 TT 像素 |
| A10 | 自定义链接分支 | landing_page_id=0 节点部署的创意 URL = 插值后裸 URL（无 /a/），不受本方案影响 |
| A11 | 清理联动 | 造一条"广告已删超 N 天"的 active link 跑 cleanup → 进 archived（回收站可见、可恢复） |

smoke 脚本形态：sandbox/真实小额账户部署 1 条带落地页广告 → A1-A3 程序断言 → 真实点击（或 `?_pv=` 预览 token + 手填 ad=test123 类探测，注意预览跳过防护但仍走 route_next）→ A5-A8 断言。

### 4.7 修复清单（实施时按序，本文档不改码）

1. **P0 B1**：`ad_ops.py:226`、`launch_templates.py:2220` 单花括号 → `{{ad.id}}`（一行×2，先于一切）。
2. **P0 B2**：部署 runner 自动建链（§4.1/4.2/4.3），落 `LaunchJobItem.subcode_slug`。
3. **P1 B5**：部署端服务端解 base（`_resolve_page_base` 口径）；编辑器 public_url 口径与 bound_subdomains 对齐。
4. **P1 B3/B4**：树预检补子码存在性检查（对齐平铺 786-794）；runner slug 查询过滤 `status.in_(reserved, active)`。
5. **P1 B6**：route_next TT 像素按账户（§4.5）。
6. **P2**：B7（随每广告粒度自然消除）、B8（TT 浏览器 event_id）、B9（随服务端解 base 消除）、B11（部署门校验 redirect_mode）。

---

## 5. 风险

| 风险 | 评估 | 缓解 |
|---|---|---|
| **链接爆炸量** | 每广告粒度下 200 广告/账户/次 × N 账户 × 高频部署；子码表与 `landing_events` 行数线性涨；每小时 `fb-check-batch`/fb_ban_scan 并发 scrape 的对象数变大（CF Pages worker 无限流但 Graph scrape 有配额） | `_TREE_ADS_MAX=200` 已封顶单次；死亡广告联动归档（4.4）；FB 屏蔽扫描按"页下 active 子码"抽样/增量而非全量（实施时核对 guard_engine fb_ban_scan 现有粒度） |
| **FB 审核页变化检测** | 每广告不同 slug URL = 同域大量 path 变体；FB 审核快照拍的是 /a/→302→目标页链。落地页内容变化（改 target/像素）时，FB 可能因"广告落地页与过审时不符"重审/拒登 | 落地页配置变更已有发布自检+health 告警；方案不改这条既有链；文案上提醒：过审后大改 target_urls 有重审风险（既有风险，非本方案引入） |
| **宏不替换（B1 残留）** | 若 FB 对双花括号也因 objective/placement 不替换（极少数版位），归因退化为无 ad_id | 部署后 A4 断言读回创意 URL；worker 对字面量 `{{ad.id}}`/`{ad.id}` 的 visit 直接告警（ingest 守卫扩到单花括号） |
| **建链与建广告非原子** | 建链成功建广告失败 → 孤儿 reserved 链接 | 14 天闲置自动归档（既有），无需事务补偿 |
| **重试翻倍** | retry 重建广告时旧 slug 已 active 绑旧 ad | retry 用"reserved 且本 item 所建"复用 + 新建并存审计；树模式重试仅限全败 item（现状已限制） |
| **多租户 slug 唯一** | slug 全局 unique（models/launch.py:88），自动建链随机 6 位全租户碰撞概率上升 | `_gen_slug` 已查重+重试；量大后可升 7 位（36^6≈21 亿，当前量级足够） |

---

## 6. 关联事实索引（读码定位表）

- 前端编辑器联动：`frontend/src/views/LaunchTemplates.vue:337-347`（平铺）、`:1068-1080`（树节点）、`:2485-2496/:2631-2641`（UI）、`:2902`（预检警告渲染）
- 子码 CRUD：`backend/app/routers/subcodes.py:18-26`（slug 生成）、`:29-58`（generate）、`:174-244`（update/delete/restore）、`:300-343`（FB 封禁检测）
- 部署链：`backend/app/routers/launch_templates.py:786-794`（平铺预检子码检查）、`:913-1061`（树预检，无子码检查）、`:1251-1259`（TT 像素用 landing_page_id）、`:2114-2121/2216-2220/2269-2276`（树子码解析/effective_url/回绑）、`:2348-2353`（平铺 link 解析）、`:1713-1734`（部署后 ads_cache 对账）
- 三件套 effective_url：`backend/app/core/ad_ops.py:223-226`（FB，单花括号）、`:361-364`（TT `__AID__`）；`backend/app/routers/launch.py:173-184`（手动路径，双花括号）
- worker/页面模板：`backend/app/routers/landing.py:44-79`（页面 HTML：_d 解码/像素/eventID）、`:82-200`（WORKER_SOURCE：/a/ 解析 146-148、redirect 176-181、display+_d 182-198）、`:285-375`（发布注入）、`:786`（public_url 口径）
- route_next：`backend/app/routers/landing_events.py:296-469`（目标 317-351、FB 像素 353-406、TT 像素 417-429、CAPI 430-438、event_id+S2S 439-460）；ingest 首点绑定 `:163-172`
- S2S：`backend/app/core/tk_events.py`（TT Events API / FB CAPI 同 UUID 双发）
- guard：`backend/app/services/guard_engine.py:2842-2902`（autobind 1h）、`:2905-2975`（cleanup 14d/30d）
- 模型：`backend/app/models/launch.py:83-94`（LandingAdLink）、`backend/app/models/launch_template.py:115`（死列）
- 1.0 对标（广告级自动子码生产验证）：`index.html:14104`（"自动生成广告级子码 /a/子码?ad={{ad.id}} 回绑广告"）

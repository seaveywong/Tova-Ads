# 方案：版位选择 + 转化位置（1:1 三层结构模板组卡补齐）

> 2026-09-08 调研产出。**只调研，未改任何代码。** 用户反馈两个缺口：
> ① 组卡版位没得选；② 转化位置只有网站。
> 事实分三档：✅ 查证过（代码通读 / Meta 官方文档摘录交叉）｜🔶 推断（代码路径分析或常识，未实测）｜❓ 待实测（官方文档被网络策略挡住，搜索摘要里含个别不确定枚举，上线前用真实 API 冒烟校准）。

---

## 一、现状差距表

| # | 能力 | 现状 | 差距 | 证据（行号） |
|---|---|---|---|---|
| 1 | 组卡版位 | **完全没有**。组节点字段里无任何版位字段，组卡无版位 UI 区块 | 平铺模式遗留了一套完整版位 UI（平台勾选+设备+细分位置树），但 FB 模板已一律强制结构模式，**该 UI 对 FB 不可达（死代码）** | `LaunchTemplates.vue` L2296-2332（版位 UI 在平铺卡内）；L1280-1281（FB 一律 tree 模式）；L779-785（blankTreeAdset 无版位字段） |
| 2 | 组卡转化位置 | **只读文本**，按 objective 默认值显示（SALES→网站、LEADS→ON_AD…），不可选 | 无单选控件；`destination_type` 无法在结构模板里设置（平铺卡的 DEST_TYPES 下拉同样是死代码 L2205） | L2077-2083（ro-field 只读）；L407-410（convLocationText 推导）；L162-169（OBJ_DEFAULTS 写死 dest） |
| 3 | 版位经 advanced_config 兜底 | 保存路径把 `manual_placement/placement_platforms/*_positions` 合并进模板级 `advanced_config.targeting.*`，但 tree 模式下这些值恒为空/false，且 **manual_placement=false 时主动 delete 全部版位键** | 结构化版位键无法经保存链存活；tree 模式编辑器也没有 advanced_config 输入框 | L1398-1433（保存合并/清除）；L2375（advanced_config textarea 只在平铺卡） |
| 4 | promoted_object 矩阵 | 只覆盖 4 组组合（SALES+网站像素 / LEADS+Instant Forms / LEADS+网站 / ENGAGEMENT 主页赞+消息类） | 无 WhatsApp、IG 私信、电话(Call Ads)、应用（application_id/object_store_url）目的地 | `ad_builder.py` L230-279 |
| 5 | 组卡 optimization_goal 自由下拉 | 组卡可选 OPT_GOALS 全集，无 objective 兼容性校验（前后端都没有） | 可选出 FB 必拒组合（见隐患 A/B） | L2148-2154；`launch_templates.py` _validate_structure 无校验 L92-201 |
| 6 | 跟帖克隆带回版位 | `_clone_ad_settings` 已回读源广告 publisher_platforms/device_platforms/facebook_positions | 但回填目标 `form.*` 是平铺字段，tree 模式下又是死值 | `fb.py` L748-755；`LaunchTemplates.vue` L700-717（applyClonedSettings 写 form） |

### 顺带发现的隐患（🔶 推断：代码路径分析，未实测）

- **隐患 A（destination_type 覆盖冲突）**：tree 部署把 `destination_type_override=tpl.destination_type` 无条件传入（`launch_templates.py` L2080）。前端 watch 会在选 objective 时自动填 `form.destination_type`（ENGAGEMENT→ON_PAGE）。于是组卡把优化目标覆盖成 `CONVERSATIONS` 时：build_adset 先按矩阵设 `destination_type=MESSENGER`（L270），随后被 override 覆盖成 `ON_PAGE`（L282-283）→ 且 L286 的 MESSENGER 版位注入判断在覆盖**之后**执行、条件不再成立 → 连 `publisher_platforms=[messenger]` 也不注入。结果 ON_PAGE+CONVERSATIONS 的组合疑似被 FB 拒。
- **隐患 B（SALES+非网站优化目标缺 promoted_object）**：组卡在 SALES 模板下选 `CONVERSATIONS` 等优化目标 → build_adset 的 SALES 分支只处理 OFFSITE_CONVERSIONS，不设 promoted_object → FB 报"必须提供 promoted_object"。
- **隐患 C（advanced_config 浅合并）**：`merged_adv = {**_tpl_adv, **adv_node}`（L2065）是**浅合并**——节点级 advanced_config 若带 `targeting` 键，会把模板级 `targeting` 的其他子键整体顶掉。后续版位若继续走 advanced_config 通道会踩这个坑；建议结构化字段优先。

---

## 二、代码现状（关键事实速查）

### 后端 `backend/app/core/ad_builder.py` — build_adset（L154-292）

- `destination_type` / `promoted_object` 按 (objective, optimization_goal) 写死（L230-279）：
  - SALES+OFFSITE_CONVERSIONS → `{pixel_id, custom_event_type}` + WEBSITE（pixel 缺失 raise）
  - LEADS+LEAD_GENERATION → `{page_id}` + ON_AD；LEADS+OFFSITE_CONVERSIONS → `{pixel_id, LEAD}` + WEBSITE
  - ENGAGEMENT+PAGE_LIKES → `{page_id}` + ON_PAGE；+CONVERSATIONS/MESSAGING_* → `{page_id}` + MESSENGER；+LINK_CLICKS/LANDING_PAGE_VIEWS → WEBSITE
  - TRAFFIC+LINK_CLICKS/LANDING_PAGE_VIEWS → WEBSITE；AWARENESS → 无 promoted_object
- `destination_type_override` 显式覆盖（L282-283）——目前 tree 部署用它传 `tpl.destination_type`。
- **MESSENGER 特例**（L285-287）：destination_type 最终为 MESSENGER 时 `targeting.publisher_platforms=["messenger"]`（整体覆盖）。执行顺序在 `extra` 深合并**之前** → advanced_config 里的 targeting.publisher_platforms 可反把它盖掉。
- `extra`（advanced_config）深合并进 payload（L289-291）；targeting 深拷贝防多账户循环 mutation 污染（L192）。

### 后端 `backend/app/routers/launch_templates.py`

- `_validate_structure`（L92-201）：组节点白名单（key/name/enabled/budget_usd/pixel_id/audience_id/audience_json/optimization_goal/billing_event/advanced_config/budget_type/lifetime_budget_usd/schedule_start/schedule_end/pacing/bid_amount_usd/minimum_roas/ads）。**未知键丢弃** → 新字段必须加白名单+枚举校验。
- `_deploy_item_fb_tree`（L1937-2290）：`build_adset(objective=tpl.objective, conversion_goal=tpl.conversion_goal, destination_type_override=tpl.destination_type, extra=merged_adv)`（L2069-2085）——转化位置由模板级三件套决定，节点只能覆盖 optimization_goal/billing_event。
- `_run_deploy_job` 分发（L2402-2413）：批量生成 → 结构树 → 单模板；**FB 模板现在基本全走 tree 链**。
- `_parse_advanced`（L1441-1449）：模板级 advanced_config JSON → dict（坏 JSON 静默 None）。

### 前端 `frontend/src/views/LaunchTemplates.vue`

- 平铺遗留资产（可直接复用到组卡）：`PLATFORMS`（4 平台+细分位置枚举 L187-203）、`DEVICES`（L204）、placement-tree 组件结构（L2306-2330）、toggle 逻辑（L207-247）、i18n keys 已全有（`launch.js` zh L33/97-99、en L244/308-310：placement/advPlusPlacement/manualSelect/pos_*）。
- `DEST_TYPES` 7 值常量（L140-143）只被平铺卡使用；`convGoalsForObjective` 是"转化事件（像素 custom_event_type）"不是"转化位置"（L144-159）。
- 组卡「转化设置」区（L2077-2088）：转化位置只读 + 像素绑定模板级 `form.pixel_id`（非节点级——节点 pixel_id 字段存在但 UI 未露出，部署链有 `snode.pixel_id` 优先级 L2073）。

---

## 三、FB API 事实（Meta Marketing API，v25 口径）

> 说明：developers.facebook.com 直连被网络策略挡，以下来自官方页面的搜索摘录 + 多来源交叉；个别枚举标注 ❓。

### 3.1 版位 targeting 字段（adset.targeting 内）

✅ 字段集：`publisher_platforms` / `device_platforms` / `facebook_positions` / `instagram_positions` / `messenger_positions` / `audience_network_positions`（+ threads 系）。

✅ `publisher_platforms` = `facebook` / `instagram` / `audience_network` / `messenger` / `threads`。
✅ `device_platforms` = `desktop` / `mobile`。
✅ `audience_network_positions` = `classic` / `instream_video` / `rewarded_video`。
✅ `messenger_positions` = `messenger_home` / `story` / `sponsored_messages`（`messenger_inbox` 已弃）。
❓ `facebook_positions` 主流值：`feed` / `right_hand_column` / `marketplace` / `story` / `video_feeds` / `instream_video` / `facebook_reels` / `search` / `facebook_profile_feed`（不同 API 版本有增删；v26 起移除 IG Explore Feed 与 Messenger Stories 作可选项的报道存在——**上线前以实测为准**）。
❓ `instagram_positions` 主流值：`stream` / `story` / `explore` / `reels` / `ig_search` / `profile_feed`（`explore_home`/`shop`/`effect_tray` 出现在部分文档快照）。

✅ **Advantage+ 版位（自动版位）语义**：targeting 里**完全省略**这些版位字段 = Advantage+ placements（系统在全部可用版位自动分配）。要手动必须显式传数组——不存在单独的"automatic_placements 开关字段"。即：我们现在不传 = 已经是 Advantage+ 自动版位，这与现 UI 文案一致。
✅ 规则：指定某平台的 `*_positions` 时，`publisher_platforms` 必须包含该平台，否则报错。

### 3.2 destination_type 与 promoted_object

✅ `destination_type`（adset 字段）现行枚举含：`WEBSITE` / `APP` / `MESSENGER` / `WHATSAPP` / `INSTAGRAM_DIRECT` / `APPLINKS_AUTOMATIC` / `ON_AD` / `ON_PAGE`（`ON_POST`/`ON_VIDEO`/`ON_EVENT`/`ON_INSTANT_EXPERIENCE`/`ON_FACEBOOK` 为遗留值；读取时可能出现 `UNDEFINED`）。
✅ `promoted_object` 按目标决定必填子字段（官方 Ad Set, Promoted Object 页）：网站转化类 = `pixel_id` + `custom_event_type`；主页/Instant Forms/消息类 = `page_id`；应用 = `application_id` + `object_store_url`；目录类 = `product_set_id` 等。
✅ Meta 按 objective × destination_type × optimization_goal × promoted_object 做"Objective Validation"，不合法组合建 adset 时直接报错（blame field）。
✅ LEADS 目标允许的目的地集合（官方 destination_type 页摘要）：网站（WEBSITE）/ 即时表单（ON_AD）/ Messenger / WhatsApp / 电话 / 应用（❓摘要中出现 `LEAD_FROM_MESSENGER`/`LEAD_FROM_IG_DIRECT` 字样，与 destination_type 取值的关系待实测确认——我们映射用 MESSENGER/INSTAGRAM_DIRECT + 对应 optimization_goal 即可）。

🔶 推断（未逐字核实，与官方指南方向一致）：
- Click-to-Messenger / WhatsApp 广告：`destination_type=MESSENGER/WHATSAPP` + `promoted_object={page_id}`（+ 目的地侧字段如 WhatsApp 号码与主页的绑定）。
- 通话广告（PHONE_CALL 目的地）属 LEADS/SALES 家族。
- SALES 的"商店"转化位置（Shops/Advantage+ shopping）走 product_set/ASC 体系，v25 起 ASC API 弃用换 Advantage+ campaign experience——**超出 v2 链路范围，不进 P0/P1**。

### 3.3 objective × 转化位置 → API 组合矩阵（方案采用子集）

| objective | UI 转化位置选项 | destination_type | optimization_goal | promoted_object | 备注 |
|---|---|---|---|---|---|
| 销量 SALES | 网站（默认） | WEBSITE | OFFSITE_CONVERSIONS | pixel_id + custom_event_type | 现行 |
|  | Messenger 私信 | MESSENGER | MESSAGING_PURCHASE_CONVERSION | page_id | 版位须含 messenger |
|  | WhatsApp / 电话 | WHATSAPP / PHONE_CALL | 对应 messaging/conversions 组合 | page_id | ❓组合细节待实测 |
| 潜在客户 LEADS | 网站 | WEBSITE | OFFSITE_CONVERSIONS | pixel_id + LEAD | 现行 |
|  | 即时表单（默认） | ON_AD | LEAD_GENERATION | page_id | 现行；联动表单模板 |
|  | Messenger / WhatsApp / IG 私信 | MESSENGER / WHATSAPP / INSTAGRAM_DIRECT | CONVERSATIONS 或 LEAD_GENERATION | page_id | 🔶组合待实测 |
|  | 电话 | PHONE_CALL | （通话类） | page_id | ❓ |
| 流量 TRAFFIC | 网站（默认） | WEBSITE | LINK_CLICKS / LANDING_PAGE_VIEWS | — | 现行 |
|  | 应用 | APP | LINK_CLICKS / APP_INSTALLS | application_id+object_store_url | P2（应用链路未建） |
| 互动 ENGAGEMENT | 主页（默认 ON_PAGE） | ON_PAGE | PAGE_LIKES | page_id | 现行 |
|  | 网站 | WEBSITE | LINK_CLICKS / LANDING_PAGE_VIEWS / POST_ENGAGEMENT | page_id(可选) | 现行 |
|  | Messenger / WhatsApp / IG 私信 | MESSENGER / WHATSAPP / INSTAGRAM_DIRECT | CONVERSATIONS | page_id | 联动消息模板 |
| 知名度 AWARENESS | （无转化位置） | — | REACH / IMPRESSIONS | — | 不出选项 |
| 应用推广 APP_PROMOTION | 应用 | APP | APP_INSTALLS / VALUE | application_id + object_store_url | P2 |

---

## 四、方案

### 4.1 字段映射表（UI → structure 节点 → API）

组（adset）节点新增字段（`_validate_structure` 白名单同步）：

| UI（组卡） | structure 节点字段 | 默认 | → API |
|---|---|---|---|
| 转化位置单选（含"自动"） | `conv_location: ""` ∈ {"",website,on_ad,messenger,whatsapp,instagram_direct,phone_call,on_page,app} | ""（=按 objective 默认，**行为与现状完全一致**，存量模板零迁移） | `destination_type` + `optimization_goal` 推导 + `promoted_object`（builder 矩阵） |
| 版位：Advantage+ 自动 / 手动 | `placement_mode: ""` ∈ {"",auto,manual} | ""（=auto，省略版位键） | auto → 不传任何版位键；manual → targeting 下写键 |
| 手动-平台勾选 | `publisher_platforms: []` ⊆ {facebook,instagram,messenger,audience_network}（threads 后续） | 空 | `targeting.publisher_platforms` |
| 手动-设备勾选 | `device_platforms: []` ⊆ {desktop,mobile} | 空（=不限） | `targeting.device_platforms` |
| 手动-细分位置（P1） | `facebook_positions/instagram_positions/messenger_positions/audience_network_positions: []` | 空 | `targeting.*_positions` |

校验规则（后端 `_validate_structure` + 前端同口径）：
- `conv_location` 不在该 objective 允许集合内 → 400（前端动态选项天然不会给出，后端兜底防 API 直调）。
- `*_positions` 传了某平台但 `publisher_platforms` 不含该平台 → 400（对齐 FB 校验，提前给中文错误）。
- `placement_mode=manual` 且 `publisher_platforms` 为空 → 400（等同全平台？不允许——必须至少选一个，避免"手动但空"歧义）。
- conv_location 与组级 `optimization_goal` 手动覆盖冲突时：**显式 conv_location 优先**，optimization_goal 覆盖值若与 conv_location 不兼容 → 400（修掉隐患 A/B 的根因）。

### 4.2 UI 方案（组卡）

**A. 转化位置（替换现只读行 L2077-2083）**
- 单选组（seg 按钮或 el-radio-group），选项按系列层 objective 动态出（矩阵 §3.3），第一项固定「自动（按广告目标）」。
- 默认=自动 → 显示推导结果文案（现 convLocationText 逻辑保留为"自动"态的预览）。
- promoted_object 联动：
  - website → 显示像素输入（节点级 pixel_id，露出现有的 `snode.pixel_id`，模板级为回退默认）+ 转化事件（SALES 用模板级 conversion_goal）。
  - on_ad（即时表单）→ 广告节点的表单模板下拉联动（lead_form_template_id 已存在，提示去广告节点配）。
  - messenger/whatsapp/instagram_direct → 提示广告节点消息模板（message_template_id 已存在）；Messenger 时版位区自动强制含 messenger 平台（不可全取消）。
  - on_page / phone_call / app → 文案提示所需输入（app 给"暂未开放"占位，P2）。
- 每个选项 hover/次行 hint 一句话说明（i18n 新 key：convLoc_auto/website/on_ad/messenger/whatsapp/instagram_direct/phone_call/on_page/app + 各 hint）。

**B. 版位（组卡新增区块，位置在受众区之后）**
- 双卡 seg：「Advantage+ 自动版位（推荐）」/「手动选择」——复用现成 i18n key `launch.advPlusPlacement`/`launch.manualSelect`。
- 手动态（P0）：
  - 设备 chips：desktop / mobile（复用 DEVICES + placement-chips 组件样式）。
  - 平台级勾选：Facebook / Instagram / Messenger / Audience Network 四个平台 checkbox（复用 PLATFORMS 常量的顶层）。
  - 勾选 Messenger 平台时若转化位置=messenger → 锁定不可取消；未勾 messenger 且转化位置=messenger → 自动勾上（FB 硬约束）。
- 手动态（P1）：每平台展开二级细分位置 chips（复用现 placement-tree 结构与 pos_* 文案，从 `form.*` 迁移绑定到节点 `s.*`）。
- 默认态=自动 → 卡内一句话："由 Meta 自动在全部版位分配（Advantage+ placements）"。

**C. 模板级兜底**
- 系列段（第一段）不放版位/转化位置（destination_type 本来就是 adset 字段，FB 也是组级设置）；平铺死代码（L2165-2378 中版位/DEST_TYPES/advanced_config 相关）P1 一并清理，避免再误导。

### 4.3 后端改动点清单

| 文件 | 改动 |
|---|---|
| `core/ad_builder.py` build_adset | ① 新参数 `conv_location: str = ""`、`placements: dict \| None = None`（结构化版位：publisher_platforms/device_platforms/*_positions）；② promoted_object/destination_type 矩阵扩列（messenger/whatsapp/instagram_direct/phone_call 分支，page_id 必填校验）；③ `destination_type_override` 语义收紧：仅在无 conv_location 时生效（tree 链传空），从根上修隐患 A；④ MESSENGER 特例改为**并入**既有 publisher_platforms（去重，不整体覆盖）；⑤ placements 写入 targeting（在 extra 深合并之前，advanced_config 仍可兜底覆盖——保持现有优先级约定） |
| `routers/launch_templates.py` `_validate_structure` | 组节点白名单加 `conv_location/placement_mode/publisher_platforms/device_platforms/{4平台}_positions`；枚举校验 + §4.1 规则（positions 需含平台 / manual 需非空平台 / conv_location×objective 兼容） |
| `_deploy_item_fb_tree` | build_adset 传 `conv_location=snode.get("conv_location")`、`placements=...`；`destination_type_override` 改传空（矩阵由 conv_location 驱动；存量无 conv_location 的模板行为不变——builder 内部默认推导与现行一致） |
| `_run_deploy_job` / `_retry_one` | 同步确认 retry 链（L2876 也传了 destination_type_override）口径一致 |
| 兼容性 | 无需迁移：新字段全可选、默认空=现状；structure 存量 JSON 不动 |

### 4.4 分期建议

- **P0（一次上线）**：组卡「转化位置」动态单选 + promoted_object/destination_type 联动（网站/即时表单/Messenger/WhatsApp/IG 私信/电话/主页，按 objective 出；app 占位提示 P2）+ 修隐患 A/B（optimization_goal 兼容校验）+ 组卡「版位」双卡但只做**平台级勾选+设备**。落地后 smoke：结构模板带 conv_location=messenger + manual 版位（含 messenger 平台）真建 adset 断言回读 targeting/destination_type（资金零消耗：节点默认 PAUSED）。
- **P1**：细分位置二级勾选（*_positions，枚举以实测校准为准）+ 跟帖克隆版位回填到树节点（applyClonedSettings 改写当前选中组）+ 平铺版位死代码清理 + threads 平台。
- **P2（另行立项）**：应用转化位置（application_id/object_store_url 全链路）、Shop/Advantage+ shopping。

---

## 五、事实分类清单

**✅ 查证过（代码通读）**：本文件第一节/第二节全部内容（含 3 个隐患为代码路径静态分析的确定性结论——逻辑成立，但"FB 是否真的拒绝该组合"未实测）。

**✅ 查证过（Meta 官方文档摘录，多来源交叉）**：3.1 版位字段集与 publisher_platforms/device_platforms/audience_network_positions/messenger_positions 枚举；省略版位字段=Advantage+ 自动版位；positions 需平台在 publisher_platforms 内；destination_type 现行枚举主体值；promoted_object 按目标必填子字段；Meta 对不兼容组合强制校验。

**🔶 推断（未实测）**：LEADS/ENGAGEMENT 消息类目的地的 optimization_goal 具体取值组合（CONVERSATIONS vs LEAD_GENERATION）；WhatsApp/电话目的地的 promoted_object 侧字段；隐患 A/B 的 FB 侧实际报错行为。

**❓ 待实测（上线前校准）**：facebook_positions/instagram_positions 完整现行枚举（v25→v26 有移除报道）；`LEAD_FROM_MESSENGER`/`LEAD_FROM_IG_DIRECT` 与 destination_type 的关系；threads 平台的 positions 字段名。校准方式：建测试 adset 后 GET `?fields=targeting` 回读，或 `/act_X/targetingsentencelines`。

## 六、需要用户拍板

1. **P0 转化位置开放范围**：建议首期=网站/即时表单/Messenger/IG 私信/WhatsApp/电话/主页（矩阵 §3.3 除 app 外全部）。其中 **WhatsApp/电话需要主页绑 WhatsApp 号码等前置配置且组合细节属🔶推断**——是首期就开，还是 P0 先开 网站/即时表单/Messenger/主页 四个已验证路径、WhatsApp/电话进 P1？（影响后端矩阵校验表和 UI 选项文案量，工作量差别不大，主要是风险面。）
2. 次要确认（不阻塞）：版位 P0 只做平台级勾选（细分位置 P1）是否符合预期；转化位置放**组级**（一个模板不同组可不同位置，对齐 FB Ads Manager 的组级设置）。

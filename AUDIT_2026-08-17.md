# ToveAds 2.0 全系统代码审计报告（2026-08-17）

> 方法：8 个并行深度审计 agent（routers×3 + services + core + 安全专项 + 多租户隔离专项 + 前端），全部只读。本报告为去重合并后的汇总；各 agent 原始输出见会话记录。
> 统计：**P0 × 8 · P1 × 47 · P2 × 111**（去重前 P1×58 / P2×151，跨 agent 重复报的已合并）。

---

## 🔴 P0（8 条 — 必修，按危害排序）

### 1. `SET LOCAL` RLS 上下文随 commit 蒸发 — 批量写/紧急暂停/刷新全灭
`core/deps.py:50` 用 `SET LOCAL app.tenant_id`（只活到当前事务）；请求中途任何 `db.commit()` 后，同 session 再查 RLS 表 = 静默 0 行。
- `routers/ads.py:291-305` batch-status >1 条时第 2 条起全挂"无可用写令牌"
- `routers/ads.py:101-114` list_ads refresh=1 刷新后列表全空
- `routers/guard.py` emergency_pause（**kill-switch 实际不可用**）
- `routers/fb.py:118/179/298` 三处 reassociate 静默空转（孤儿要等 2h watchdog 才自愈）
- **修法**：改 `SELECT set_config('app.tenant_id', :tid, false)`（会话级，不受 commit 影响，靠连接归池 rollback 清理）；或写路径纪律性禁止请求中途 commit。

### 2. 限流冷却永不自动恢复 — 一次 code-17 令牌永久出池，止损静默停摆
`core/fb_tokens.py:33-41` `_is_cred_available` 先判 `status != "active"` → `mark_cred_cooldown` 改 status="rate_limited" 后**全库无任何复位路径**（grep 确认只有手动检测端点会重置）。单令牌租户一次限流 → 巡检对该租户全部账户跳过、无告警。i18n 文案承诺"30 分钟自动恢复"是假的。
- **修法**：`_is_cred_available` 改为 `status=="rate_limited" and cooldown_until < now` 视为可用；或 watchdog 扫描复位。

### 3. JWT 无吊销 + 滑动续期无限续命 — 权限回收永久失效
`core/security.py:35-44` + `main.py:67-80` + `core/deps.py:42-57`：token 只验签名/过期，**完全信任 payload 的 tenant_id/role，不复查 membership**；renew_token 原样搬运 claims → 被移除成员/降权/改密后，旧 token 活跃用一天续一天**永久保有原权限**。
- **修法**：加 `token_version` 列或 jti 会话表；get_current_user 复查 membership；改密/移除/降权时失效旧 token；TG deep-link 绑定改用短效专用 token（现在 `/start <JWT>` 泄露渠道=7 天账号接管，`routers/tg_webhook.py:82-120` + `routers/notify.py:284-291`）。

### 4. Worker TDZ — 防护开启时根路径 100% ReferenceError
`routers/landing.py:127-150`（WORKER_SOURCE 内嵌 JS）：非 `/a/` 分支在 `const _isPreview` 声明前引用它 → `block_enabled=true` 时**落地页本体直接 500**。自检矩阵只测 `/a/` 没覆盖根路径。
- **修法**：`_pv/_isPreview/cf/ip/ua` 声明提到函数最前；`_worker_check.mjs` 补根路径用例。

### 5. 预算告警整个 job 从未工作过
`services/budget_alerts.py:48` 调用**不存在的方法** `fb.get_adset_insights` → AttributeError 被 except 吞成 warning → 预算进度告警 100% 死功能，从未被人发现。
- **修法**：实现 `get_adset_insights`（level=adset）或改 `get_insights(level="adset")`；**连带**：`budget_alerts.py:71-74` 本币 spend 直接除 minor-units 预算（USD 缩小 100 倍永不告警），需 `from_minor_units` 换算。

### 6. 部署/重试 runner SuperSessionLocal 无 tenant 过滤 — 跨租户读写（花钱路径）
`routers/launch_templates.py:520/526/333 + 712-722`：BYPASSRLS 下查 `Asset/LandingAdLink/SavedAudience/LaunchTemplate/LaunchJobItem` 均无 tenant 过滤 → 租户 A 填 B 的 asset_id/subcode_slug/audience_id 或直接构造 retry → **读走 B 的素材文案、把 B 的子码绑到 A 的广告、用 B 的定向**。
- **修法**：runner 内所有 sdb 查询补 `tenant_id`；模板创建/更新时校验引用归属。

### 7. 素材上传无类型/大小校验 — 同源存储型 XSS + 磁盘耗尽
`routers/assets.py:149-155` + `main.py:141`：ext 直接取自文件名（.html/.svg 可传），落 `/static-assets` 同源服务 → **偷 localStorage JWT**。
- **修法**：扩展名+MIME 白名单；加 `X-Content-Type-Options: nosniff` + CSP；限制大小。

### 8. AI 纠偏在巡检热路径同步调用 — token 消耗失控 + 巡检被拖死
`services/kpi_resolver.py:46-87` + `guard_engine.py:420/450`：每广告每轮调 `resolve_kpi` **两次**，L4/L5 路径触发同步 LLM 调用（60s 超时）→ 5min 一轮 × N 广告 × 2 次。
- **修法**：AI 纠偏按 (objective, opt_goal) 缓存 + TTL；tick 分支复用评估分支已算结果。

---

## 🟠 P1 精选（47 条，按主题分组；完整明细见各 agent 原始输出）

### 跨租户/越权（7）
| 位置 | 问题 |
|---|---|
| `routers/fb.py:528` | `_fetch_post_content` PagePost 查询无 tenant 过滤（page_posts 无 RLS）→ 跨租户读帖内容 |
| `routers/leads.py:79` | `/leads/sync` 任意 form_id 可拉别家表单潜客进自己租户 |
| `routers/landing_lib.py:316-364` | CF zones/域名导入端点缺 `is_superadmin` 门（普通租户可看平台全部 zones、导入任意根域） |
| `services/guard_engine.py:1409` | run_keepalive Asset 查询无 tenant 过滤（BYPASSRLS）跨租户拿素材 |
| `routers/launch.py:94` + `launch_templates.py:196` | 部署目标账户无 is_managed/导入校验 → 未纳管账户建广告（违反显式导入硬规则+无止损覆盖） |
| `routers/landing.py:116-126`+`landing_events.py:123-197` | 公开 ingest 无防灌：伪造 visit/click 污染统计、抢绑子码 ad_id、灌 IP 打 dedup |
| `routers/auth.py:64-83` | 失败登录日志硬编码 tenant_id=1（跨租户 PII 落租户可读表）+ 登录无限速可暴力破解 |

### 止损/巡检可靠性（8）
| 位置 | 问题 |
|---|---|
| `routers/fb.py:274-290` | check_credential 的 transient 豁免逻辑写反：临时 FB 抖动把令牌永久判 expired（无恢复路径） |
| `core/fb_client.py:222-243` | `get_ad_insights` only_active=True 恒空（fields 缺 effective_status）→ **trend_drop 规则静默全灭** |
| `services/guard_engine.py:718-731` | coverage_lost 告警用循环残留的最后 tenant_id → 发错租户+压制真盲区租户的告警 |
| `services/kpi_resolver.py:147-151` | L5 AI 字段不在 actions 时 conversions=0 → click_no_conv 等规则误停广告 |
| `services/ad_ops.py:126/193/250` | advisory lock key 用进程内 `hash()` → 跨 worker 不互斥（并发写同广告） |
| `services/guard_engine.py:57-84` | `_campaign_objectives` 没请求 optimization_goal 字段 → L4 矩阵恒 miss，KPI 实际只跑两级 |
| `services/guard_engine.py:1429-1444` | 保活用 daily_budget 而设计是 lifetime（$5/天无上限，且 VND 硬编码百分数） |
| `routers/guard.py:275-286` | manual_inspect 改模块全局 COOLDOWN_MIN（多 worker 竞态）；平台级手动触发无租户隔离 |

### RLS/数据层（4）
- 9 张租户表无 RLS 策略：`page_posts / ads_cache / leads / ad_redirect_overrides / launch_templates / launch_jobs / launch_job_items / message_templates / lead_form_templates`（当前靠 ORM 过滤兜着，一个迁移补齐）
- `routers/launch_templates.py:188-211` 部署并发/重复无守卫（双击=双份广告双份预算；retry 不校验状态可对 running/success 项重跑）
- `routers/launch_templates.py:509-511` BackgroundTasks 非持久，重启即丢 → job 卡 running + FB 侧广告继续花钱，无 stale-job 回收
- `routers/fb.py:925-938` 重导入主令牌 flip-flop + skipped 计数失真；`fb.py:88/105` token_type 更新被 schema 默认值静默降级回 "user"

### FB API 层（5）
- `core/fb_client.py:93-119` POST 超时无幂等保护重试 → 重复建广告/帖（ReadTimeout 服务端可能已执行）
- `core/fb_client.py:361-364` get_ad_creative_links 不翻页 → >200 广告子码漏绑；`guard_engine.py:1106/1389` 同病
- `core/fb_client.py:70-78`+5 处 `FbApiError(msg, 0)` 双参构造 → 错误路径抛 TypeError 吞掉真实报错（page_post.py:38/49、ad_ops.py:162、launch_templates.py:545/743）
- `services/account_sync.py:61-154` 全程单一大事务，末尾一次 commit，中途异常全丢
- `routers/launch.py:859-906` 防护测试模拟与 worker 实现不一致（模拟漏爬虫拦截/required_query/datacenter_block）

### 发布/落地页（5）
- `routers/landing.py:575-597` 发布失败留幽灵 draft 行；`:292-294` block_html 校验放行但 worker 从不渲染
- `routers/landing_events.py:397-407` 自检/发布触发**真实 TK S2S 转化事件**（污染 TK 数据）
- `routers/landing_lib.py:402-478` zip 模板 resources 从未部署（css/js/图全丢，按规范传包的用户页面残废）
- `routers/landing.py:1656-1669` source-stats 全量捞行进 Python 循环（百万级打爆）
- `routers/landing.py:131-138` 防护开启但规则为空时连内置爬虫拦截都短路

### 认证/安全（6）
- `routers/rbac.py:233-253` change_member_role 缺最后 owner 保护 → 可把唯一 owner 降级锁死团队
- `routers/rbac.py:127-135` 自定义角色改名不迁移成员 → 一批成员权限静默清零；`:220-226` 邀请默认密码明文返回 + must_change_password 有完整 API 权限
- `routers/auth.py:177-191` 改登录邮箱不要旧密码（账户接管链）
- `routers/fb_oauth.py:194-195` callback 把原始异常渲染到公开页
- `core/config.py:17` fb_cred_key/jwt_secret 无强度校验不 fail-fast
- `routers/tg_webhook.py:58` 未用 TG 官方 secret_token 机制，callback 无 chat_id 身份校验

### 前端（8 条 P1，0 P0）
- `Dashboard.vue:355` ElMessageBox 未 import → **"立即采集"按钮静默死**；`:769` copyIds 未定义 → 复制按钮渲染错误
- `Dashboard.vue:595-738` 趋势图与 KPI 卡范围脱钩三处（自定义区间/账户多选/preset 回点不刷图表）
- `Dashboard.vue:43-51` 请求无序列化，慢旧响应覆盖新数据
- `Assets.vue:81` 硬编码生产 API 域（绕过 env，本地开发打生产库）
- `Guard.vue:104/130` 把**翻译文本**持久化进规则表（i18n 数据污染：中文环境建的规则英文用户看是中文）
- `Members.vue:152` 角色 select 非受控，取消后显示错角色
- `Landing.vue:431` `landing.saveFirst` key 两份 locale 均缺失（唯一缺失 key）

---

## 🟡 P2 汇总（111 条，主题聚类）

性能类（~22）：AdsCache JSON 全表扫热点（ingest/route_next/diagnose/_local_resolve，建议倒排索引表）· N+1（pixel_usage/landing 页计数/admin 租户计数/i18n locale）· source_stats/source 全量进 Python · `_CACHE`/`_RR_STATE` 无上限 · isDirty 全量 JSON.stringify · sequential 轮换 COUNT(*)。
校验缺失（~19）：subcode status/ad_id 校验 · compliance status 枚举 · compliance/auth register 密码强度/邮箱格式 · settings .env 写入不转义 · form_templates fb_form_id 无唯一约束 · date_from 格式。
一致性（~15）：notify last_7d 口径两套 · dashboard total_roas 算术平均 · `/logs` UTC 日 vs 北京业务日 · refresh 不过滤 is_managed · account_status 排序与看板原则反。
死代码/杂项（~30）：Dashboard 死代码群 · LaunchTemplates preflight 死功能 · landing_lib 重复路由装饰器 · last_active_at 死列 · nonce 死字段 · FRONTEND_URL 硬编码 · python-jose CVE 建议换 PyJWT · CORS 生产含 localhost · 无安全响应头 · form_templates AI 无配额 · _ai_auto_create_form 不经人审 · i18n 英文界面中文标点残留 ~15 处 · en.js langToZh 中文 tooltip。

## ✅ 核对无问题的
SQL 全参数化无注入 · subprocess 全 list 形态 · zip 解压有防护门 · 密钥不入 git · 前端 0 XSS sink（0 v-html，HTML 弹窗全转义）· 路由守卫完整 · i18n 2133 key 两份全同步（1 缺失）· advisory lock 101-111 无重号+release 严格配对 · 通知 dedup 纪律好 · is_managed 过滤全链一致 · RLS fail-closed 设计成立（SET LOCAL+池归还是 rollback，无脏连接）· 假停核验/BLEED_ABORT/fill-forward 等 1.0 成熟机制完好。

## 修复优先级建议（三批）

**第一批（本周，P0×8 + P1 里 5 个"花钱/停摆"级）**：
1. P0-1 set_config 会话级 RLS（一行改 deps，救批量写+紧急暂停+refresh）
2. P0-2 冷却自动恢复（一行改 _is_cred_available，救止损可用性）
3. P0-4 Worker TDZ（挪声明顺序，救防护开启的落地页）
4. P0-5 预算告警 get_adset_insights（补方法+币种换算）
5. P0-6 部署/retry runner 补 tenant 过滤（救跨租户写）
6. P0-7 素材上传白名单+nosniff
7. P0-8 AI 纠偏缓存
8. P1：trend_drop 恒空（fields 加 effective_status）+ FbApiError 构造修正 + POST 重试幂等 + coverage_lost 租户分桶 + Dashboard 两个死按钮

**第二批（下周）**：JWT 吊销/token_version + TG 短效绑定 token + 登录限速 + 改邮箱要旧密码 + 9 表 RLS 迁移 + 部署并发守卫 + stale-job 回收 + leads form_id 白名单 + CF 域名库超管门 + ingest 防灌

**第三批（择期）**：其余 P1（保活 lifetime、账户导入校验、protection 模拟对齐、zip resources、幽灵 draft…）+ P2 按主题批量清

## 总体评价
架构纪律显著好于同类（RLS fail-closed、双日期基准、通知去重、advisory lock、前端零 XSS sink、i18n 全同步），但存在**三个从未暴露的全局性静默失效**（预算告警死功能、限流永不恢复、trend_drop 恒空）+ **一个系统性 RLS 事务边界缺陷**击穿批量写与紧急暂停 + 部署链路的信任边界松（跨租户引用无校验）。核心规律：**新功能快速堆叠时绕过了自家约定**（leads/ads_cache 等新表漏 RLS、launch runner 忘 tenant 过滤、上传忘白名单）。

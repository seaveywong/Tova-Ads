# TovaAds 资金安全审计报告（2026-09-04，五维并行）

> 触发：当日漏网亏损事故（哨兵+紧急暂停后仍有系列投放数小时）。
> 方法：5 个并行 Agent 深扫（止损引擎/部署创建/覆盖盲区/数据决策/前端+告警），全部有代码行依据。
> 结论分三级：🔴P0 直接资金损失 / 🟠P1 高概率风险 / 🟡P2 次级。

---

## 一、汇总：P0 共 13 项（按危险度排序）

| # | 维度 | 问题 | 亏损场景 | 位置 |
|---|---|---|---|---|
| P0-1 | 数据决策 | **budget_burn_fast 把已是 USD 的快照再除汇率** → 非 USD 账户全部广告当日累计跨 $20 即被"瞬烧"误停 | VND/IDR 账户正常投放在几小时内被批量错杀（USD 账户无症状所以从未暴露） | guard_engine.py:274 vs :1647 |
| P0-2 | 数据决策 | **to_usd 未知币种兜底汇率=1.0** → COP/CLP 账户花几分钱被判烧了几百万 | 几美分真实消耗即触发空耗暂停，全账户广告一轮杀光；KWD 被低估 3 倍漏止损 | guard_engine.py:1744-1752 |
| P0-3 | 数据决策 | **AI 纠偏可把非零转化清零**（缓存 1h）| click_no_conv 类规则（无挡板）误杀实际有 purchase 的广告，同 objective 广告连续 1h 全中招 | kpi_resolver.py:163-171 |
| P0-4 | 止损引擎 | **CBO 扩量冷却键与预算载体错位** → campaign 无限加预算 | CBO 系列 5min 一轮 +15% 复合增长，"24h 冷却"形同虚设；cap=0 时无界 | guard_engine.py:386-411 vs :491-507 |
| P0-5 | 覆盖盲区 | **account_status≠1 的账户三道护栏全弃**（宽限期 9 仍在烧钱、政策受限 7 常仍投） | 欠费宽限期 3-7 天自由烧钱零监控；紧急暂停恰恰此刻停不掉它 | guard_engine.py:1495/2104, guard.py:348 |
| P0-6 | 覆盖盲区+止损 | **哨兵全线静默失败**（无令牌/拉取失败/pause 失败全部只进进程日志，零告警零留痕）+ **armed 账户同时被巡检跳过** = 双通道同灭 | 令牌权限被 BM 收回 → 哨兵 3min 一轮全失败无人知晓 → 系列持续投放数小时（本次事故的引擎侧同型未修路径） | guard_engine.py:2111-2160, 1506-1508 |
| P0-7 | 覆盖盲区 | **unmanage 后广告继续投放零告警零留痕**（active_ads 计数只进 HTTP 响应即丢） | 移除纳管的账户 FB 侧照常花钱，系统彻底不管，审计痕迹被主动销毁 | fb.py:1276-1346 |
| P0-8 | 覆盖盲区 | **租户全令牌失效 → 整租户零监控且 orphan 告警链饿死**（reassociate 的租户列表同样只查 active creds → still_orphan 永不产生） | 60 天 data_access 过期 → 所有账户持续投放无止损，只剩每日一条 warning | guard_engine.py:1466/1946-1949 |
| P0-9 | 部署创建 | **部署重试零幂等**（中断的 item 重试 = 同账户再建一整条链） | 部署中途重启 → 12 个 FB 已建成账户全部重试 → 双份广告双倍预算 | launch_templates.py:1224-1241, fb_client.py:129-134 |
| P0-10 | 部署创建 | **预算全链无上限校验**（前后端都只查 >0） | $100 打成 $10000 × N 账户直达 FB，当天烧穿 | services/ad_ops.py:179-184, launch_templates.py:63-97 |
| P0-11 | 部署创建 | **零小数币种两套冲突表 → 100× 预算放大**（部署路径 vs 手动改预算路径用不同表，对称差 14 币种） | ISK/XAF 账户手动改预算瞬间放大 100 倍且展示层看不出来 | core/ad_ops.py:16-19 vs services/ad_ops.py:23 |
| P0-12 | 告警可靠 | **storm cap 压掉的告警永久丢失**（dedup 已先行落库=当天不再补发；暂停失败的 rule_pause 也被压） | 出血日 40+ 条止损动作，第 31 条起通知全灭——含"暂停失败广告继续花钱"的 critical | notify_utils.py:159-161 + NO_CAP 缺 rule_pause fail/budget_progress_98 |
| P0-13 | 告警可靠 | **TG 单发无重试无降级** + **critical 只路由 owner**（operator 绑了 TG 也收不到 token 失效/巡检停滞） | TG 被墙期间所有 critical 告警 100% 失声；日常运营是 operator 绑定场景下系统级警报送错人 | notify_utils.py:211-222, 109-111 |

## 二、P1 共 19 项（高概率风险，摘要）

**止损/引擎侧**：假停核验只覆盖 ad 级（adset/campaign 级零核验+核验读失败当成功→60min 成功冷却不重试）；哨兵 1h dedup 按日志不按实际状态（重激活 55min 复燃）；预算通道假错误重试放大（同 pause 虚警未修到预算）；加白跳过快照写入（历史断档+基线重置）；扩量锁外读基数可覆盖手动下调。

**覆盖侧**：TT 预算告警双路径全缺（TtClient 无 get_adset_insights→AttributeError 被吞）；TT 孤儿无告警链；巡检非 token 类失败静默；学习期 24h 含默认止血也不生效（且 created 缺失 fail-open）；保活前缀=用户可蹭的免死金牌；紧急暂停终验不含 TT；哨兵拉系列 limit 200 不翻页。

**数据侧**：resolver 异常 conv 强制归 0（fail-closed=杀广告）；_BROAD_ACTIONS 缺私信类字段；AI 同步阻塞热路径 60s 无负缓存（AI 半死→巡检拖到小时级→lock_busy 连锁→止损整体失明）；budget_alerts 把 minor units 当主币（两位小数币种预算告警永不触发）；预付费余额≤0 静默不告警。

**部署侧**：防重 check-then-act 竞态（双击=双 job）；半成品无清理已建 ID 不落库（孤儿 ACTIVE campaign 带预算）；无预算模板静默回退 $2000/天；汇率无合理性校验；reaper 只在启动跑一次。

**前端侧**：预算编辑零防护（无上限无确认无币种显示）；批量开启无确认+整行点击即选中；哨兵 armed N/M 不可见（armed=0 显示"开"）；部署确认框不报总预算。

## 三、结构性根因（三个，修一处护全部）

1. **`account_status == 1` 被当作"在花钱"的代理条件**——宽限期(9)/受限(7)/未结清(3) 都还能花钱，但被巡检/哨兵/预算/紧急暂停四路同时排除。改为死状态黑名单（仅 2/8 跳过）。
2. **armed 与巡检互斥 + 哨兵失败不告警**——armed(=用户要全停) 的账户失去巡检兜底，而哨兵自身三类失败静默。修：哨兵失败发 critical；armed 账户连续 5 轮零成功暂停发 critical；或取消互斥。
3. **所有告警链上游以"存在 active 凭证"为前提**——凭证全灭时连告警链本身一起死（orphan 链饿死）。修：watchdog 加"有 managed 账户但零 active 凭证"租户扫描发 critical。

## 四、覆盖矩阵（谁在什么条件下还看着账户）

| 账户状态 | 巡检 | 哨兵 | 预算 | 余额/状态 | ads_cache |
|---|---|---|---|---|---|
| managed+status1+令牌健康 | ✅ | 仅armed | ✅ | ✅ | ✅ |
| +armed+无pause令牌 | ❌ | ❌静默 | 视read | ✅ | 视read |
| status 9/7/3 | ❌ | ❌ | ❌ | ✅一次 | ❌ |
| 全租户令牌死 | ❌ | ❌ | ❌ | ❌ | ❌(1条/日) |
| unmanaged | ❌ | ❌ | ❌ | ❌ | ❌ |

## 五、修复批次建议

- **批R1（当日）**：P0-1/2（两处一行级数据口径修复，直接影响当前 VND 等账户的止损正确性）+ P0-4（扩量冷却键）+ P0-6（哨兵失败告警+armed 告警）+ P0-13（TG 重试+critical 路由放宽）——全是行级改动，当天可部署。
- **批R2（次日）**：P0-5/7/8（覆盖三根因）+ P0-9/10/11（部署幂等/预算上限/币种表统一）+ P0-12（NO_CAP 补漏+storm suppressed 可见）。
- **批R3（本周）**：P0-3（AI 纠偏挡板）+ P1 全量按序。

## 附：做得对的（审计确认无需改）

fb_client POST 超时不自动重放；retry 原子抢占；部署/重试双重纳管守卫；紧急暂停逐系列回读核验+FB 虚报不轻信（今日已修）；暂停失败自动缩短冷却重试+升级链；规则引擎单账户单线程无并发加预算；get_paged 全量翻页无截断；加白日期三处统一账户本地日；扩量有 cap 钳制+lifetime 不动；CPA/扩量强制 FB 转化不受 landing 稀释；除零守卫齐全。

# 平台付费体系方案（钱包 + 域名闭环 + 对账）— 2026-09-26 稿

> 状态：**设计稿，待拍板**。批1 实现约：后端 ~450 行 + 前端 ~350 行 + 2 张表迁移。

## 一、现状盘点
| 环节 | 现状 | 痛点 |
|---|---|---|
| 域名购买 | USDT 直付（平台收款地址 + 订单尾号对账）→ **超管人工确认** → 自动注册 | 每单人工；小额多单链上费高；不能自动续费 |
| 余额/充值 | 无 | 所有自动扣款场景被堵（续费 cron #378、月费） |
| 平台总账 | 超管订单列表可见全部（本批已加团队/下单人列） | 无钱包流水、无对账 |
| 退款 | 无 | — |

## 二、目标闭环（用户视角）
1. **充值**：owner/finance 在钱包页发起 USDT 充值（金额自选，复用 TronGrid 监听 + 尾号对账）→ 链上到账 → **自动入账**（免人工）+ 通知
2. **下单**：余额充足 → 下单**即时扣款、零确认、全自动注册交付**（注册链已有失败重试）
3. **兜底**：余额不足 → 提示充值；也可选 USDT 直付单（保留现有流程，到账后超管确认——可配「到账自动确认」开关）
4. **退款**：仅超管，退回余额（流水留痕）
5. **平台**：超管看全平台钱包余额 + 全部流水 + 链上对账页

## 三、数据模型（2 张表）
```
wallet_accounts: tenant_id(唯一) / balance_usd / version(乐观锁) / updated_at
wallet_txns:     id / tenant_id / type(deposit|withdraw|charge|refund|adjust)
                 / amount_usd(正数，type 表方向) / balance_after(每次记账后余额快照)
                 / ref_type(domain_order|topup|monthly|manual) / ref_id
                 / txid(链上) / status(pending|confirmed) / note / created_by / created_at
                 唯一约束 (ref_type, ref_id, type) —— 幂等防重复扣/重复入账
```
**铁律**：余额变更只走 `wallet_apply()` 单一入口（SELECT FOR UPDATE + version 校验 + balance_after 断言）；充值到账回调与扣款共用；RLS 租户隔离。

## 四、与现有系统的接缝
- **TronGrid 监听复用**：usdt_monitor 现在按「收款地址 + 尾号金额」匹配订单；扩展匹配 `充值单`（同样尾号模式，充值单金额用户自选含尾数）
- **订单流**：`create_order` 时若余额 ≥ total → 直接 charge + status 跳 `approved` 进注册链；否则维持 pending_payment（直付兜底）
- **approve**：余额单无此状态；直付单保留人工（或开自动）
- **权限**：看钱包=billing.view（现有权限位）；充值=owner/finance；退款/调整=超管；沿用「账户派生事件带租户」的通知铁律

## 五、分批交付
**批1（拍板后做）**：表 + wallet_apply 入口 + 充值（自动入账）+ 余额下单全自动 + 钱包页（余额/充值/流水）+ 超管对账页 + 直付兜底保留
**批2（等批1 跑稳）**：域名到期自动续费（cron #378 解锁，余额不足提前通知）+ 月费/服务费定期扣款 + 账单 CSV 导出 + 充值优惠（赠额）如果商业化要

## 六、不做/缓做
- 法币支付通道（Stripe 等）：等有合规主体再说，USDT 先行
- 自动提现（withdraw）：只做流水记录 + 超管线下打款后核销，不接链上出金
- 多币种：全程 USD 计价（USDT 只是通道）

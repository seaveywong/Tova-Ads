# 广告管理器（AdManager）交接文档 — 2026-09-09

> 给接手的 AI / 开发者。当前广告管理器**功能可用但不完美**，以下是完整现状、已知问题、和改进方向。

## 一、架构概览

```
前端 AdManager.vue（~1000行）
  ├── 三层 Tab（系列/组/广告）+ 潜客 Tab
  ├── 数据源：GET /ads/list（读 ads_cache 表，非实时拉 FB）
  ├── 实时核验：⚡ Live Verify（逐条查 FB 开关状态，秒级）
  ├── 写操作：暂停/恢复/改预算/改名（走后端→FB API）
  └── 脱管/被禁标签 + 排序沉底

后端 ads.py
  ├── /ads/list — 从 ads_cache 读三层结构（15min 缓存）
  ├── /ads/refresh — 后台全量拉 FB 三层（1-2分钟）
  ├── /ads/{id}/status — 改状态
  ├── /ads/{id}/budget — 改预算
  └── /ads/diagnose — 广告诊断

数据流：FB API → 巡检(5min) / cache_sync(15min) → ads_cache → /ads/list → 前端
```

## 二、当前已实现

| 功能 | 状态 | 说明 |
|---|---|---|
| 三层浏览 | ✅ | 系列→组→广告，点击系列名钻取 |
| 状态开关 | ✅ | ACTIVE/PAUSED 切换（走后端） |
| 改预算 | ✅ | 日/总预算，含安全上限 |
| 改名 | ✅ | 三层均可 |
| 实时核验 | ✅ | ⚡ 按钮秒级查开/关状态 |
| 全量刷新 | ✅ | 后台 1-2 分钟 |
| 脱管/被禁标签 | ✅ | 黄标/红标 + 顶部警示条 |
| 脱管排序沉底 | ✅ | 正常在前 |
| 成效双列 | ✅ 后端 | results_fb（FB口径）+ conversions（综合） |
| 潜客 Tab | ✅ | Lead 列表 + 状态管理 |
| 筛选/搜索 | ✅ | 状态筛选 + 名称/ID 搜索 |
| 移动端 | ✅ | 基本可用 |

## 三、已知不完美（按优先级）

### P1 — 体验缺口

1. **数据时效标注不够醒目**
   - "数据更新至 XXX" 在页面顶部，但用户不注意
   - 脱管账户的数据是**最后快照**，用户以为是实时的
   - **改进**：脱管行加时间戳标注（"快照于 06:36"）

2. **成效双列前端展示刚上线，数据还没流**
   - `results_fb` 需要下一轮巡检成功才有值
   - 在那之前 fallback 到 `conversions`（会比 FB 显示的多）
   - **改进**：等令牌恢复数据流入后验证数字对齐

3. **三层导航面包屑不够直觉**
   - 钻取后返回方式不够明显
   - **改进**：加面包屑导航条

4. **列宽在小屏幕挤压**
   - 广告层列太多（消耗/成效/CPA/触达/频次/落地/CTR/点击）
   - **改进**：列可配置显示/隐藏

5. **搜索只搜名称和 ID**
   - 不能搜账户名、系列名（跨层搜索）
   - **改进**：全局搜索

### P2 — 功能缺口

6. **无批量操作确认反馈**
   - 批量暂停/恢复后没有 toast 汇总
   - **改进**：批量操作后显示成功/失败计数

7. **广告创意预览不稳定**
   - 缩略图有时拉不到（FB 端限制）
   - 大图查看器已有但触发不直觉
   - **改进**：统一缩略图→点击→大图交互

8. **预算编辑内联 vs 弹窗不一致**
   - 日预算是内联编辑，总预算是弹窗
   - **改进**：统一为内联

9. **无排序记忆**
   - 刷新后排序回到默认
   - **改进**：localStorage 持久化

### P3 — 数据层

10. **ads_cache 15分钟缓存 vs 巡检 5分钟不一致**
    - 巡检回写快，cache_sync 回写慢
    - 两个数据源可能有时间差
    - **改进**：统一为一个数据流

11. **脱管账户数据永远停在最后快照**
    - 无自动清理或标注过期
    - **改进**：超过 24h 的快照加"过期"标记

## 四、代码位置速查

| 要改什么 | 文件 | 关键函数/位置 |
|---|---|---|
| 列表渲染/筛选/排序 | `frontend/src/views/AdManager.vue` | `curList` computed (~L249) |
| 脱管标签 | 同上 | `accStateTag()` (~L40) |
| 脱管排序 | 同上 | `_deadRank` in curList |
| 成效显示 | 同上 | 搜 `results_fb` |
| 数据加载 | 同上 | `load()` (~L95) |
| API 路由 | `backend/app/routers/ads.py` | 全文件 |
| 缓存同步 | `backend/app/services/ads_cache_sync.py` | `run_ads_cache_sync` |
| 巡检回写 | `backend/app/services/guard_engine.py` | 搜 `ads_cache` |

## 五、部署命令

```bash
# 前端（改 .vue 后）
cd /d/dev/Mira_One/toveads/frontend
npm run build
CLOUDFLARE_API_TOKEN=<CF_TOKEN> npx wrangler pages deploy dist --project-name tovaads --branch master

# 后端（改 .py 后）
cd /d/dev/Mira_One
export MSYS_NO_PATHCONV=1 MIRA_SSH_HOST=45.76.177.37 MIRA_SSH_USER=root MIRA_SSH_PASS=<SSH_PASS>
node _putx.js toveads/backend/<file> /opt/toveads/backend/<file>
node _sshx.js 'cd /opt/toveads/backend && venv/bin/python -m py_compile <file> && venv/bin/python -c "from app.main import app" && systemctl restart toveads && curl -s http://127.0.0.1:8000/health'
```

## 六、铁律（违反出事）

1. **改代码先 commit 再部署**（回滚安全网）
2. **restart 前必须 py_compile + import 双门**
3. **通知必带 dedup_recent**（否则 spam）
4. **i18n zh/en 成对、en 零 CJK、无字面 `{{`**（vue-i18n 编译 throw）
5. **Vue 模板用 `{{ }}` 双花括号**（单/三花括号都不渲染）
6. **build 后必须验证产物包含新代码再部署**（build 失败不报错只部署旧版）
7. **无保护期**（学习期默认 0，止损优先）
8. **资金操作必须二次回读**（暂停后回读 effective_status 确认）
